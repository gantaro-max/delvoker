# 仕様詳細

実装済み機能の詳細仕様をまとめたドキュメントです。

---

## 1. パーティ編成システム

### 1.1 構成

| 役割 | クラス | 最大人数 |
| :--- | :--- | :---: |
| プレイヤー | `Player` (extends `Status`) | 1 |
| 臨時NPC | `NPCMember` (extends `Status`) | 2 |
| **パーティ合計** | `Party` | **3** |

### 1.2 Party クラス仕様 (`data.py`)

| メンバー / プロパティ | 型 | 説明 |
| :--- | :--- | :--- |
| `members` | `list[Status]` | index 0 = プレイヤー、以降 NPC |
| `add(member)` | `bool` | 満員時 `False`、追加成功時 `True` |
| `is_wiped_out` | `bool` | 全員 HP ≤ 0 なら `True`（全滅判定） |
| `alive` | `list[Status]` | HP > 0 のメンバー一覧 |
| `fallen` | `list[Status]` | HP ≤ 0 のメンバー一覧（死亡メンバー） |
| `MAX_SIZE` | `int` | `3`（定数） |

### 1.3 NPCMember クラス仕様 (`data.py`)

```
NPCMember(job_key, name, personality)
```

| フィールド | 型 | 有効値 / 説明 |
| :--- | :--- | :--- |
| `personality` | `str` | `"normal"` \| `"reckless"` \| `"cowardly"` \| `"selfish"` |
| `inventory` | `list` | アイテム所持（現在は空） |
| `is_unique` | `bool` | `False`（デフォルト）/ `True` = 固有NPC（手動操作可能） |

### 1.4 固有NPC昇格システム

- **場所:** ギルド画面（街メニュー > Guild）
- **費用:** `PROMOTION_COST = 1000G`
- **効果:** `npc.is_unique = True` に設定
- **戦闘での変化:** 昇格済み固有NPCは `STATE_BATTLE_NPC_CMD` でコマンド選択（Fight / Retreat）が可能になる。未昇格NPCは引き続きAI自動行動。

---

## 2. NPC AI 行動システム

### 2.1 発動条件

- 戦闘中、プレイヤーの行動後に各 NPC メンバーが順番に行動する。
- 未昇格 NPC は毎ターン自動で行動し、性格と状況に応じてスキルを使用する。

### 2.2 性格別 AI 行動とスキル使用優先度

| 性格 | キー | スキル使用条件 | スキル発動確率 | 通常行動 |
| :--- | :--- | :--- | :---: | :--- |
| 無謀 | `reckless` | 攻撃スキル所持 + MP 足りる | **40%** | 通常攻撃 |
| 臆病 | `cowardly` | 回復スキル所持 + MP 足りる + 味方 HP 50%以下 | **80%** | 何もしない（行動スキップ） |
| 普通 / 利己的 | `normal` / `selfish` | スキル所持 + MP 足りる | **20%** | 通常攻撃 |

**スキル選択ロジック:**
- `reckless`: `effect_type == "attack"` のスキルのみ対象。ランダムに1つ選択。
- `cowardly`: `effect_type == "heal"` のスキルのみ対象。HP 最低の味方を回復。
- `normal`: 攻撃 or 回復スキルをランダム選択。回復時は HP 最低の生存メンバーを対象。

**スキルダメージ / 回復量:** `max(1, skill.power + npc.mag)`

### 2.3 ログメッセージ規則（ASCII制約）

- スキル使用: `[Skill] {name} uses {SkillName}! {target}: -{dmg} HP!`
- スキル回復: `[Skill] {name} uses {SkillName}! {target}: +{heal} HP!`
- 無謀の通常攻撃: `[Reckless] {name} attacks! {enemy}: -{dmg} HP!`
- 臆病スキップ: `[Cowardly] {name} hesitates and does nothing!`
- 通常攻撃: `{name} attacks! {enemy}: -{dmg} HP!`

---

## 3. 属性相性システム（三すくみ）

### 3.1 ATTR_AFFINITY 定数 (`data.py`)

```python
ATTR_AFFINITY = {"fire": "ice", "ice": "poison", "poison": "fire"}
```

`ATTR_AFFINITY[A] = B` は「A は B 属性を持つ対象に特効」を意味する。

### 3.2 ダメージ計算優先順位

| 優先順位 | 条件 | 倍率 |
| :--- | :--- | :---: |
| 1 | `weapon.attribute` in `target.weaknesses` | ×1.5 |
| 2 | `weapon.attribute` in `target.resistances` | ×0.5 |
| 3 | `ATTR_AFFINITY[weapon.attribute]` in `target.resistances` | ×1.5 |
| 4 | `weapon.attribute == "holy"` (耐性なし) | ×1.2 |
| 5 | その他 | ×1.0 |

### 3.3 防具エンチャントシステム

#### EnchantedArmor クラス (`data.py`)

| フィールド | 型 | 説明 |
| :--- | :--- | :--- |
| `prefix` | `dict \| None` | 接頭辞（`def_bonus_mod` を持つ） |
| `suffix` | `dict \| None` | 接尾辞（`attribute` を持つ） |
| `attribute` | `str \| None` | 付与属性（装備者の耐性に自動追加） |
| `rarity` | `str` | `"magic"` or `"common"` |

#### 防具エンチャントテーブル (`enchants.json`)

**接頭辞（armor_prefixes）:**

| キー | 名称 | 効果 | ウェイト |
| :--- | :--- | :--- | :---: |
| `sturdy` | Sturdy | `def_bonus` +2 | 30 |
| `light` | Light | `def_bonus` -1 | 20 |

**接尾辞（armor_suffixes）:**

| キー | 名称 | 耐性属性 | ウェイト |
| :--- | :--- | :--- | :---: |
| `of_heat` | Heat | fire | 20 |
| `of_cold` | Cold | ice | 20 |
| `of_venom` | Venom | poison | 15 |

#### Status.resistances プロパティ (`data.py`)

`Status.resistances` はダイナミックプロパティ。  
`self._resistances`（基本耐性リスト）に `self.armor.attribute`（装備防具の属性）を自動的に含めて返す。

---

## 4. スキルシステム

### 4.1 Skill クラス仕様 (`data.py`)

```python
Skill(name, mp_cost, effect_type, power)
```

| フィールド | 型 | 説明 |
| :--- | :--- | :--- |
| `name` | `str` | スキル名（ASCII） |
| `mp_cost` | `int` | 発動に必要なMP |
| `effect_type` | `str` | `"attack"` \| `"heal"` |
| `power` | `int` | ダメージ/回復量の基礎値 |

### 4.2 GrimoireItem 仕様 (`data.py` / `items.json`)

| フィールド | 型 | 説明 |
| :--- | :--- | :--- |
| `skill_name` | `str` | 習得するスキル名 |
| `mp_cost` | `int` | スキルのMP消費 |
| `effect_type` | `str` | スキルのタイプ |
| `power` | `int` | スキルの威力 |

### 4.3 スキル習得ルール

- `Status.skills` の最大スロット数: `MAX_SKILLS = 4`
- 魔導書（Grimoire）使用時: `Skill` を生成して `skills` リストへ追加
- スロット満杯時: `skills.pop(0)`（最古を削除）して新スキルを末尾に追加
- 戦闘コマンド画面: 習得済みスキル一覧を `Skill:` ラベルとともに表示

---

## 5. 全滅・敗北処理

- **全滅条件:** `party.is_wiped_out` が `True`（全パーティメンバー HP ≤ 0）
- **現在の敵ターゲット:** 敵は常にプレイヤーを攻撃する（将来的に全メンバーをターゲットに拡張予定）
- **敗北後の処理:** 全パーティメンバーの HP / MP を最大値に回復し、街へ強制帰還する
- **ロストルーレット演出:**
  - 対象: `player.inventory` の各アイテム + 装備中の武器 + 装備中の防具
  - 選択: ランダムに1つを選択して消去
  - 演出: `ITEM LOST: [Item Name]...` のログ出力 + `flash_timer = 20`（赤フラッシュ20フレーム）

## 6. ダメージポップアップ（Phase 4実装済み）

### 6.1 カラー定義

| 種別 | 色定数 | パレット番号 |
| :--- | :--- | :---: |
| 通常ダメージ | `COL_WHITE` | 7 |
| 弱点ヒット | `COL_YELLOW` | 10 |
| 被ダメージ（プレイヤー） | `COL_RED` | 8 |
| 回復 | `COL_GREEN` | 11 |

### 6.2 実装仕様

- `App.popups`: `{"text", "x", "y", "color", "timer"}` の辞書リスト
- `add_popup(text, x, y, color)`: timer=20 で追加
- 毎フレーム timer をデクリメント、0 になったリストから除去
- 描画時: `float_y = y - (20 - timer)` で上方向フロート

---

## 7. 状態異常システム（Phase 4実装済み）

### 7.1 Status.status_effects 仕様

```python
self.status_effects = {"poison": 0, "stun": 0}  # 残りターン数
```

| 異常 | キー | 効果 | 付与源 |
| :--- | :--- | :--- | :--- |
| 毒 | `"poison"` | 敵ターン終了時に2ダメージ、残ターン-1 | Slime (30%) |
| スタン | `"stun"` | プレイヤーの行動を1ターンスキップ | Skeleton (20%) |

### 7.2 アイコン表示位置

- プレイヤー HP バー右側 (x=108, y=134)
- NPC HP 表示右側 (各メンバー個別)
- `[P]` = 緑 (COL_GREEN)、`[S]` = 黄 (COL_YELLOW)

---

## 8. マップ自動生成（Phase 4実装済み）

### 8.1 Map クラス仕様 (`data.py`)

| メソッド / プロパティ | 説明 |
| :--- | :--- |
| `generate_random(width=20, height=20)` | 部屋と通路アルゴリズムでランダムマップを生成 |
| `is_wall(x, y)` | 壁判定（範囲外も True） |
| `tile_at(x, y)` | タイル種別を返す（範囲外は TILE_WALL） |
| `visit(x, y)` | `visited[y][x] = True` に設定 |
| `set_tile(x, y, tile)` | タイル書き換え（宝箱開封後の FLOOR 化に使用） |
| `start_x, start_y` | 最初の部屋の中心座標 |

### 8.2 生成アルゴリズム概要

1. 全タイルを TILE_WALL で初期化
2. 25 回ランダムに部屋を試行配置（重複チェックあり）
3. 隣接する部屋の中心を水平→垂直の通路で接続
4. 最後の部屋の中心に TILE_STAIRS を配置
5. 中間の部屋に TILE_CHEST を配置

### 8.3 ミニマップ表示仕様

- 位置: `(SCREEN_W - width - 2, VIEW_H - height - 2)` = (234, 154) for 20×20 map
- 1 ピクセル = 1 タイル
- 色分け: 現在地=白、フロア=グレー、階段=黄、宝箱=橙（未訪問タイルは非表示）

---

## 9. ステータス割り振りシステム（Phase 4実装済み）

### 9.1 bonus_points

- `Status.bonus_points`: レベルアップ1回につき +3 加算
- 消費: 任意のステータスを +1 するたびに -1

### 9.2 割り振り画面仕様

- 状態: `STATE_STAT_ALLOC = 11`
- アクセス: 街メニュー「Stats」選択
- 操作: 上下で項目選択、Z でポイント消費、X で終了
- 強化可能ステータス: STR / DEF / AGI / MAG

---

## 10. 敵 AI タイプ（Phase 4実装済み）

### 10.1 ai_type 仕様

| ai_type | ターゲット選択ロジック |
| :--- | :--- |
| `"normal"` | `party.alive[0]`（プレイヤー優先） |
| `"ranged"` | AGI が最高の生存メンバーを攻撃 |
| `"support"` | HP が最低の生存メンバーを攻撃（トドメ優先） |

### 10.2 ボス予兆（telegraph）仕様

- `EnemyDef.telegraph_message` が設定されている敵が、30% の確率で1ターン前に警告メッセージを表示
- 次ターンは 2倍ダメージの強力攻撃を実行（`[POWER]` タグ付きログ）
- 現在の設定: Orc Chief が `"The Orc Chief is charging power!"` を発動

---

## 11. 部位破壊システム（Phase 4実装済み）

### 11.1 データ構造

**EnemyDef.parts（enemies.json）**

```json
"parts": [
  {"name": "Right Arm", "hp_ratio": 0.4, "weakness": "fire", "drop_flag": "chief_gauntlet"}
]
```

| フィールド | 型 | 説明 |
| :--- | :--- | :--- |
| `name` | `str` | 部位名（ASCII） |
| `hp_ratio` | `float` | 部位HP = `enemy.max_hp * hp_ratio`（最小1） |
| `weakness` | `str` | 部位の弱点属性（将来の部位別ダメージ計算に使用予定） |
| `drop_flag` | `str` | 部位破壊時のドロップアイテムキー（`ITEM_CATALOG` のキー） |

**Enemy クラス追加フィールド**

| フィールド | 型 | 説明 |
| :--- | :--- | :--- |
| `parts` | `list[dict]` | `EnemyDef.parts` の参照 |
| `part_hps` | `dict[str, int]` | `{部位名: 残HP}` |
| `broken_parts` | `set[str]` | 破壊済み部位名セット |

### 11.2 戦闘フロー

1. `STATE_BATTLE_CMD` で Fight 選択 → `_build_part_targets()` が `["Body"] + [未破壊部位名...]` を返す
2. 部位が1つ以上ある場合 → `STATE_BATTLE_TARGET_PART` へ遷移
3. 部位選択画面: 上下カーソル、Z で確定、X でキャンセル（Body 攻撃に戻る）
4. `_player_attack(part_name)` 呼び出し:
   - ダメージは敵本体HP (`enemy.hp`) に適用
   - `part_name != "Body"` の場合、同ダメージを `enemy.part_hps[part_name]` にも適用
   - 部位HP が 0 になると `broken_parts` に追加し `[PART BROKEN: {name}]` を表示

### 11.3 部位ドロップ判定（_handle_victory）

- 勝利時、`broken_parts` 内の各部位に対して **20%** の確率でドロップ抽選
- ドロップ成功: `[Part Drop] Got: {item.name}!` を表示
- バッグ満杯: `[Part Drop] Bag full! {item.name} lost.` を表示

---

## 12. トラップと環境ギミック（Phase 5実装済み）

### 12.1 トラップタイル仕様

| タイル種別 | 定数 | 値 | 効果 |
| :--- | :--- | :---: | :--- |
| Spike Trap | `TILE_TRAP_SPIKE` | 4 | 踏むと 5〜10 のランダムダメージ |
| Poison Trap | `TILE_TRAP_POISON` | 5 | 踏むと `poison` 状態異常を 3 ターン付与 |

- 外見上は `TILE_FLOOR` と区別がつかない（ミニマップ上も `COL_DARK_GRAY` で表示）
- 踏んだ瞬間に `STATE_TOWN_SUB` ダイアログでメッセージを表示
- トラップは1回踏むと解除される（`TILE_FLOOR` に変換、`set_tile` で書き換え）
- スタート地点（最初の部屋の中心座標）にはトラップを配置しない
- 配置量: 各部屋につき 0〜2 個をランダム配置（`Map.generate_random` 内で処理）

**ログメッセージ（ASCII）:**
- Spike: `"Ouch! A spike trap!"` / `"You took N damage!"`
- Poison: `"Poison needles! You are poisoned."`

### 12.2 遺品（Grave）回収フロー

1. **遺品生成:** パーティ全滅時、全滅座標に `TILE_GRAVE` タイルを配置し、ロストしたアイテムを関連付けて保存する
2. **遺品発見:** 次のダンジョン潜入でその座標に到達すると、Grave タイルが存在する場合「遺品を発見した」メッセージを表示
3. **回収戦闘:** Grave に紐づく「幽霊的な敵エンカウント」が発生する（同フロアの敵より少し強い設定）
4. **ロスト品復元:** 遺品エンカウントに勝利するとロストしたアイテムが返還される。敗北した場合は Grave タイルのまま残る

---

## 7. 戦闘画面のUI仕様

### 7.1 敵エリア（Y: 28〜108）
- 敵の矩形スプライト（赤ボックス）
- 敵名、HP数値、HPバー表示

### 7.2 プレイヤー・パーティステータス（Y: 122〜148）
- Y=122: 区切り線
- Y=126: プレイヤー名 / Lv / ジョブ / HP数値
- Y=136: プレイヤー HP バー（100px幅、色は HP 割合で変化）
- Y=142: NPC パーティメンバー HP（最大2名を横並び表示、各128px幅）

### 7.3 コマンドウィンドウ（Y: 150〜238）
- 戦闘コマンド選択 / メッセージ表示 / 勝敗結果表示

---

## 13. 街・拠点機能

### 13.1 町での復活サービス（寺院）

- **アクセス:** 町メニュー「Revive」選択 → `STATE_REVIVE = 13`
- **条件:** `party.fallen`（HP ≤ 0 のメンバー一覧）が空でない場合に選択可能

**復活仕様:**

| 項目 | 内容 |
| :--- | :--- |
| 費用計算 | `対象メンバーのレベル × 100 ゴールド` |
| 費用表示色 | `COL_YELLOW`（支払い可能） / `COL_DARK_GRAY`（不足） |
| 復活効果 | 対象メンバーの HP・MP を最大値に全回復 |
| 所持金不足 | 何も起きない（復活不可） |
| 死亡者なし | `"Everyone is healthy."` を表示 |

**操作:**
- 上下キー: 死亡メンバー選択
- Z キー: 復活実行（ゴールド消費、`revive_idx` を 0 にリセット）
- X キー: 町メニューに戻る

### 13.2 NPC への装備授与（Give to NPC）

- **アクセス:** インベントリ画面で武器 / 防具を選択 → `STATE_INV_ACTION` → 「Give to NPC」→ `STATE_INV_GIVE_NPC = 14`
- **対象アイテム:** `item.kind in ("weapon", "armor")` かつパーティに NPCMember が存在する場合のみメニューに表示

**授与フロー:**
1. アクションメニューで「Give to NPC」選択 → NPC 選択画面へ遷移
2. 対象 NPC の現在装備は自動的にプレイヤーのインベントリへ返却
3. 授与アイテムはプレイヤーのインベントリから削除、NPC の装備スロットに設定
4. NPC の `total_def`（= `def_ + armor.def_bonus`）がリアルタイムに更新される

**操作:**
- 上下キー: NPC 選択
- Z キー: 授与実行
- X キー: アクションメニューに戻る
