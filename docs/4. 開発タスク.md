# 開発タスク一覧（ロードマップ）

個人開発で挫折を防ぐため、段階的に実装を進めるタスク分解です。

## Phase 1: 基礎プロトタイプ（歩ける・戦える）
- [x] Pyxelの基本プロジェクト作成（画面サイズ、カラーパレットの初期設定）。
- [x] 一人称視点3Dダンジョンの描画テスト（ワイヤーフレーム等で、1マス進む・曲がる処理の実装）。
- [x] 基本的なターン制バトルの構築（味方1人 vs 敵1体で、攻撃してHPを減らし合う基本ロジック）。

## Phase 2: コアループの構築（潜る・戻る・成長する）
- [x] データ構造の設計（アイテムデータ、敵データ、ジョブ・ステータスクラスの作成）。
- [x] 街のメニュー画面の作成と、街⇔ダンジョンの画面遷移の実装。
- [x] **【構造強化】マスターデータのJSON外部化**：アイテム、敵、ジョブの全データをJSONから読み込む仕組みへ移行。
- [x] **【構造強化】UI Windowフレームワークの構築**：装飾枠・アニメーション・タブ切り替えを持つ汎用Windowクラスの実装。
- [x] レベルアップによるステータス自動上昇ロジックの実装（ジョブごとの成長率設定）。
- [x] アイテムインベントリ機能と、装備の着脱処理の実装。

## Phase 3: Delvoker独自の面白さの実装（ハクスラ・NPC・ペナルティ）
- [x] 武器のランダムエンチャント（接頭辞・接尾辞の付与とステータス変化）の実装。
  - [x] `data/enchants.json` の作成（接頭辞・接尾辞データ、ウェイト値）
  - [x] `data.py` に `EnchantedWeapon` クラスを追加（prefix/suffix を保持し `label()` で名前を組み立てる）
  - [x] ドロップ時にウェイト抽選でエンチャントを付与するロジックの実装
  - [x] インベントリ画面でレアリティに応じた表示色（normal / rare / epic / legend / genesis）に対応
- [x] エンカウント率の調整と戦闘属性（Attribute）システムの実装。
  - [x] `ENCOUNTER_RATE` を `0.3` → `0.15` に引き下げ（探索ストレス軽減）
  - [x] `WeaponItem` に `attribute` フィールド追加（初期値 `None`）
  - [x] `EnemyDef` に `weaknesses` / `resistances` リスト追加
  - [x] `EnchantedWeapon` で接尾辞の `attribute` を引き継ぐよう対応
  - [x] `Status`（プレイヤー）に `weaknesses` / `resistances` プロパティ追加
  - [x] `_calc_dmg` を更新（弱点×1.5、耐性×0.5、最低ダメージ1保証）
  - [x] `holy` 属性を含む属性システム基盤を構築
- [x] パーティ複数人での戦闘対応と、臨時NPCのAI行動（勝手な行動）ロジックの実装。
- [x] 固有NPCへの昇格条件処理と、昇格による操作権限解放の実装。
  - [x] ギルド画面（STATE_GUILD）でNPCを1000Gで昇格（`is_unique = True`）
  - [x] 昇格済みNPCは戦闘中に `STATE_BATTLE_NPC_CMD` でコマンド選択
- [x] 全滅時の「ランダムロストルーレット演出」と、アイテム消去処理の実装。
  - [x] `_calc_item_loss()`: インベントリ・装備武器・装備防具からランダム1つ消去
  - [x] 赤フラッシュ演出（`flash_timer = 20`）と `ITEM LOST: ...` ログ表示
- [x] 魔導書（アイテム）使用によるスキル習得・上書きシステムの実装。
  - [x] `GrimoireItem` クラス（`data.py`）と魔導書2種（`items.json`）
  - [x] `_do_use()` でスキル習得、最大4枠、満杯時は最古を自動上書き

## Phase 4: システムの深化（戦闘・探索・成長）
- [x] **属性相性システム（三すくみ）の実装**: fire > ice > poison > fire の相性倍率をダメージ計算に統合。
  - [x] `ATTR_AFFINITY` 定数の追加（`data.py`）
  - [x] `_calc_dmg` をアフィニティ対応に更新（三すくみ×1.5、holy×1.2）
  - [x] `enemies.json` に属性データ追加（Skeleton: ice 属性、Goblin: fire 属性、Bat: holy 弱点）
- [x] **防具エンチャントシステムの実装**
  - [x] `EnchantedArmor(ArmorItem)` クラスの追加（prefix/suffix、attribute耐性）
  - [x] `make_enchanted_armor()` 関数の追加（ウェイト抽選）
  - [x] `enchants.json` に armor_prefixes / armor_suffixes を追加（Sturdy/Light/Heat/Cold/Venom）
  - [x] `Status.resistances` をダイナミックプロパティ化（装備防具の属性を自動追加）
  - [x] インベントリでの `EnchantedArmor` レアリティ表示色対応（magic: 黄）
  - [x] ドロップ処理で防具は `make_enchanted_armor()` を通じてエンチャント抽選
- [x] **戦闘システムの高度化**: 状態異常(Poison/Stun)、ダメージ数値のカラーポップアップ表示。
  - [x] `Status.status_effects = {"poison": 0, "stun": 0}` 追加
  - [x] ポップアップシステム (`add_popup`, `popups` リスト, 20フレームフロート表示)
  - [x] 毒: 敵ターン終了時に2ダメージ、スタン: 行動スキップ + ログ
  - [x] 戦闘画面に `[P]`(緑) / `[S]`(黄) ステータスアイコン表示
  - [x] 敵が状態異常を付与 (Slime: poison 30%, Skeleton: stun 20%)
- [x] **マップ生成の自動化**: ランダム生成(Procedural Generation)ロジック、階段、宝箱ギミックの配置。
  - [x] `Map` クラス追加 (`data.py`): `generate_random`, `visit`, `tile_at`, `set_tile`
  - [x] タイル定数追加 (`TILE_FLOOR/WALL/STAIRS/CHEST`)
  - [x] 部屋と通路アルゴリズム (20×20マップ、最大8部屋)
  - [x] 階段・宝箱の自動配置 (各1つ以上)
  - [x] オートミニマップ (右下、1px/タイル、訪問済みのみ表示)
  - [x] 宝箱インタラクション (エンチャント付きアイテムドロップ、タイル消去)
  - [x] 階段インタラクション (次フロア生成、フロア番号表示)
- [x] **成長システムの拡張**: レベルアップ時のステータスポイント手動割り振り。
  - [x] `Status.bonus_points` 追加 (レベルアップ毎 +3)
  - [x] `Status.mag` 追加 (初期値 2)
  - [x] `STATE_STAT_ALLOC` 状態と Stats メニュー追加
  - [x] STR/DEF/AGI/MAG 割り振りUI (Z: 消費, X: 完了)
- [x] **敵AIの多様化**: AIタイプ分岐 (normal/ranged/support)、ボス予兆システム。
  - [x] `EnemyDef.ai_type` フィールド追加 (ranged/support/normal)
  - [x] `_get_enemy_target()` でAIタイプ別ターゲット選択
  - [x] `EnemyDef.telegraph_message` フィールド追加
  - [x] ボス予兆: 30%確率で警告→次ターン2倍ダメージ (`[POWER]`)
  - [x] `orc_chief` ボス敵追加 (HP:60, DEF:4, support AI, telegraph付き)
  - [x] `skeleton` NPC タイプ追加 (`NPC_TYPES`)
- [x] **部位破壊システム**: 特定部位へのダメージ蓄積によるレアドロップフラグ付与。
  - [x] `EnemyDef.parts` リスト追加（`name`, `hp_ratio`, `weakness`, `drop_flag`）
  - [x] `Enemy.part_hps` / `Enemy.broken_parts` 追加、生成時にHP初期化
  - [x] `STATE_BATTLE_TARGET_PART` 状態追加、`_build_part_targets()` で未破壊部位リスト生成
  - [x] 部位選択UI（`_upd_battle_target_part` / `_draw_battle` 内の TARGET_PART 描画）
  - [x] `_player_attack(part_name)` で部位HPへの適用と `[PART BROKEN: {name}]` ログ
  - [x] `_handle_victory` で破壊済み部位の drop_flag 別枠ドロップ判定（20%）
  - [x] `enemies.json` の `orc_chief` に `Right Arm` 部位データ追加
  - [x] `items.json` に `chief_gauntlet` 追加

## Phase 5: システム拡張と完成度向上
- [x] **NPC個別装備と高度AIの実装**:
  - [x] インベントリからNPCへの装備授与機能（`STATE_INV_GIVE_NPC`）
  - [x] 授与後に旧装備をプレイヤーインベントリへ返却
  - [x] 性格別スキル自律使用AI（reckless:40%, cowardly:80%, normal:20%）
- [x] **町での復活サービスの実装**:
  - [x] `Party.fallen` プロパティ追加（HP ≤ 0 メンバー一覧）
  - [x] `STATE_REVIVE = 13` の追加と `TOWN_MENU` への「Revive」追加
  - [x] `_upd_revive` / `_draw_revive` 実装（費用: Lv×100G、全回復）
- [x] **トラップと環境ギミックの実装**:
  - [x] `TILE_TRAP_SPIKE` タイル追加（踏むと 5〜10 ランダムダメージ）
  - [x] `TILE_TRAP_POISON` タイル追加（踏むと毒 3 ターン付与）
  - [x] マップ生成時にトラップタイルをランダム配置（各部屋 0〜2 個、スタート地点除外）
- [x] **遺品回収イベントの実装**:
  - [x] `Grave` データクラスの定義と `App` での保持 (`self.grave`, `self.is_grave_battle`)
  - [x] 全滅時に座標・フロア・アイテムを `Grave` に保存するロジック (`_enemy_turn` / `_calc_item_loss` 修正)
  - [x] `Map.generate_random` での `TILE_GRAVE` 配置 (grave/current_floor 引数追加)
  - [x] 遺品回収バトルの実装 (Revenant 追加、`_upd_dungeon` タイル判定、`_handle_victory` 回収処理、ミニマップ COL_PINK 表示)
- [x] **拠点（マイホーム）の永続強化（実装済み）**: `STATE_HOME = 15`、倉庫 Warehouse（手持ち↔STORAGE 移動）、Renovate（+5枠拡張×5段階）、Training（STR/DEF/MAG 永続強化）を実装。`Player.perm_stats` を攻撃・防御計算に反映。
- [x] クリア目標（ダンジョンコア/マスターとの最終決戦、任意テレポートスキルの付与）の実装。
  - [x] `MAX_FLOOR = 5` 定数追加（B5Fが最深部）
  - [x] B5F階段踏み込み時に `dungeon_master` ボス戦を開始するロジック
  - [x] `enemies.json` に `dungeon_master` 追加（HP:150、全属性耐性、予兆付き）
  - [x] `STATE_ENDING = 16` / `_upd_ending` / `_draw_ending` の実装（最終スタッツ表示）
  - [x] ボス撃破後に `_handle_victory` から `STATE_ENDING` へ遷移
  - [x] ボス戦では逃走を100%失敗にするガード処理
  - [x] `Skill.is_utility` フラグ追加 / `GrimoireItem.is_utility` 追加
  - [x] `items.json` に `grimoire_return`（Teleport スキル習得、800G）追加
  - [x] `STATE_DUNGEON_SKILL = 17` / `_upd_dungeon_skill` / `_draw_dungeon_skill` の実装（Sキー）
  - [x] Teleport 使用時（MP:10消費）でダンジョンから即 `STATE_TOWN` 遷移
## Phase 6: コンテンツ拡張（実装済み）
- [x] **階層拡張とボス再編**:
  - [x] `MAX_FLOOR = 10` に更新（B10Fが最深部）
  - [x] B5F階段踏み込み時に `dungeon_master`（中ボス）戦、撃破後に B6F へ進行
  - [x] B10F階段踏み込み時に `archdemon`（最終ボス）戦、撃破後にエンディング
  - [x] `_try_flee` で `dungeon_master` および `archdemon` への逃走を禁止
- [x] **新規モンスター追加** (`enemies.json`):
  - [x] `wraith` (HP:35, holy弱点, fire/ice耐性, ranged AI)
  - [x] `golem` (HP:80, DEF:5, normal AI)
  - [x] `mimic` (HP:30, DEF:3, normal AI)
  - [x] `wyvern` (HP:50, AGI高, ice弱点, ranged AI)
  - [x] `archdemon` (HP:300, DEF:8, 全属性耐性, 予兆付き最終ボス)
- [x] **新規アイテム追加** (`items.json`):
  - [x] `steel_sword` (2d8+3), `mithril_sword` (2d10+5)
  - [x] `steel_plate` (DEF+6), `antidote` (毒解除)
  - [x] `grimoire_ice` (Ice Lance, MP:5, 威力15), `grimoire_poison` (Poison Mist, MP:4, 威力10)
- [x] **階層別ドロップティア実装** (`main.py`):
  - [x] `_drop_pools(floor)` 関数: B1-3/B4-6/B7-10 の三段階
  - [x] `_open_chest` と `_handle_victory` でフロア別ティアを参照
- [x] **階層別エンカウント実装** (`main.py`):
  - [x] `_encounter_pool(floor)` 関数: B1-3/B4-6/B7-10 の三段階
  - [x] `_start_battle` と `_spawn_dungeon_npcs` でフロア別プールを使用
- [x] **ミミック実装** (`main.py`):
  - [x] TILE_CHEST 踏み込み時に20%確率でミミック戦が発生
- [x] **状態異常治療** (`main.py`, `data.py`):
  - [x] `ConsumableItem.cure_status` フィールド追加
  - [x] `_do_use` で `cure_status` を参照し `status_effects` をリセット
- [x] **ショップ拡充**: `antidote`, `grimoire_ice`, `grimoire_poison` を `SHOP_KEYS` に追加
## Phase 7: ポリッシュ（Audio & Juice）
- [x] **オーディオ初期化** (`main.py`): `_init_audio()` メソッドを `App.__init__` で呼び出し、Pyxelサウンド/ミュージックをコードで定義。
  - [x] Sound 0: Attack SE（ノイズバースト）
  - [x] Sound 1: Hit SE（ローノイズ・インパクト）
  - [x] Sound 2: Heal SE（三角波アルペジオ）
  - [x] Sound 3: Town BGM（スクウェア波メロディ）
  - [x] Sound 4: Dungeon BGM（三角波ドローン）
  - [x] Sound 5: Battle BGM（パルス波アップテンポ）
  - [x] Music 0/1/2 をそれぞれ Town/Dungeon/Battle BGMに割り当て
- [x] **SEトリガー** (`main.py`):
  - [x] `_player_attack`: 攻撃時 `pyxel.play(0,0)`、ヒット時 `pyxel.play(1,1)`
  - [x] `_enemy_turn`: 敵攻撃時 `pyxel.play(0,0)`、ヒット時 `pyxel.play(1,1)`
  - [x] `_do_use`: 回復・治療アイテム使用時 `pyxel.play(2,2)`
- [x] **BGM自動切替** (`main.py`): `_set_state` 内で `_BGM_ZONES` を参照し `pyxel.playm` を呼び出し、ゾーン変化時のみ切り替え。
- [x] **画面揺れ（Screen Shake）** (`main.py`):
  - [x] `self.shake_timer` 追加
  - [x] `_enemy_turn` でダメージ発生時に `shake_timer = 5` を設定
  - [x] `update()` で `shake_timer` をデクリメント
  - [x] `draw()` の冒頭・末尾で `pyxel.camera()` を制御
- [x] **ログカラーリング拡張** (`main.py`):
  - [x] `_show_msgs` に `colors` 引数を追加（省略時は自動判別）
  - [x] `_msg_color()` 静的メソッドでメッセージ内容に応じた色を返す（アイテム黄、警告赤、回復緑、ボス撃破ピーチ等）
## Phase 8: ダンジョンギミック（鍵扉・地図巻物）
- [x] **TILE_LOCKED_DOOR = 7** (`data.py`): 鍵付き扉タイルを定義。`Map.is_wall()` で壁扱い。
- [x] **Map.generate_random 拡張** (`data.py`):
  - [x] 通路チョークポイントを検出し、最大2か所に TILE_LOCKED_DOOR を確率 30% で配置。
  - [x] 扉が存在する場合、rooms[1] に TILE_CHEST (key_chest_pos) を追加して dungeon_key を保証。
- [x] **鍵扉インタラクション** (`main.py`):
  - [x] `_interact_locked_door(nx, ny)` ヘルパー追加
  - [x] `_upd_dungeon` の KEY_UP/KEY_DOWN でロック扉チェックを is_wall の前に実施
  - [x] Dungeon Key あり→鍵消費・扉を TILE_FLOOR に変換、なし→「It's locked.」表示
- [x] **key_chest_pos 保証** (`main.py`): `_open_chest` でキーチェスト位置なら常に dungeon_key を付与。
- [x] **Scroll: Mapping** (`data/items.json`, `main.py`):
  - [x] `scroll_mapping` アイテム追加（ConsumableItem）
  - [x] SHOP_KEYS に `scroll_mapping` 追加
  - [x] `_do_use` で使用時に全 visited を True に書き換え、「The entire floor map is revealed!」表示
- [x] **ミニマップ表示** (`main.py`): TILE_LOCKED_DOOR を COL_INDIGO で表示。
- [x] **items.json** 更新: `dungeon_key`, `scroll_mapping` 追加。
## Phase 9: ダンジョンインタラクション・動的イベント
- [x] **TILE_FOUNTAIN = 8 / TILE_MERCHANT = 9** (`data.py`): 新タイル定数を定義。`Map.is_wall()` で両タイルを壁扱い。
- [x] **Map.generate_random 拡張** (`data.py`):
  - [x] 全フロア: 50% の確率で rooms[1:] のいずれかに `TILE_FOUNTAIN` を 1 つ配置。
  - [x] B3F以降: 20% の確率で rooms[2:] のいずれかに `TILE_MERCHANT` を 1 つ配置。
- [x] **癒しの泉インタラクション** (`main.py`):
  - [x] `_interact_fountain(nx, ny)` ヘルパー追加。
  - [x] 70%: HP/MP 最大値の 30% 回復、20%: 空振り、10%: 毒付与。
  - [x] 使用後 `TILE_FLOOR` に変換。
  - [x] `_upd_dungeon` の KEY_UP/KEY_DOWN でチェックを追加。
- [x] **放浪の商人インタラクション** (`main.py`):
  - [x] `_interact_merchant(nx, ny)` ヘルパー追加 → `STATE_DUNGEON_SHOP` に遷移。
  - [x] `_upd_dungeon` の KEY_UP/KEY_DOWN でチェックを追加。
- [x] **STATE_DUNGEON_SHOP = 18** (`main.py`):
  - [x] `_BGM_ZONES` にゾーン 1 (ダンジョン BGM) で追加。
  - [x] `_set_state` に分岐追加。
  - [x] `_upd_dungeon_shop()`: 上下選択・Z購入（1.5倍価格）・X終了（タイルをFLOORに変換）。
  - [x] `_draw_dungeon_shop()`: 3Dビュー＋ステータス維持、商人パネルオーバーレイ描画。
- [x] **MERCHANT_KEYS** (`main.py`): `elixir`, `potion`, `ether`, `antidote`, `holy_scroll`, `grimoire_heal`。
- [x] **ミニマップ表示** (`main.py`): `TILE_FOUNTAIN` = COL_BLUE、`TILE_MERCHANT` = COL_GREEN で表示。
- [x] **items.json** 更新: `elixir`（全回復消費品）, `holy_scroll`（グリモア: Holy Blast 威力30）追加。

---

## Phase 10: 永続化とメタプログレッション

- [x] **data/jobs.json** 更新: `thief`（hp_die=6、agi_growth=0.9）追加。`mage` は既存。
- [x] **main.py** インポート追加: `json`, `Path`, `JOBS`, `WeaponItem`, `ArmorItem`, `ConsumableItem`。
- [x] **SAVE_FILE 定数** (`main.py`): `"delvoker_save.json"`。
- [x] **STATE_TITLE = 19, STATE_JOB_SELECT = 20** (`main.py`): 追加。`_BGM_ZONES` にゾーン 0 (Town BGM) で追加。
- [x] **Player クラス** (`main.py`): `__init__(self, job_key="warrior")` に変更。
- [x] **App.__init__** (`main.py`): `title_idx`, `job_select_idx`, `unlocked_jobs` を追加。初期ステートを `STATE_TITLE` に変更。
- [x] **_set_state** (`main.py`): `STATE_TITLE`, `STATE_JOB_SELECT` ブランチ追加。
- [x] **_upd_title / _draw_title** (`main.py`): New Game / Continue 選択画面。
- [x] **_upd_job_select / _draw_job_select** (`main.py`): 解放済みジョブ選択画面。選択後に金/倉庫/perm_stats を引き継いで新 Player を生成。
- [x] **_serialize_item / _deserialize_item** (`main.py`): EnchantedWeapon/Armor の prefix/suffix を含む完全シリアライズ。
- [x] **save_data / load_data** (`main.py`): JSON ファイル書き込み・読み込み。エラー時は無視して新規スタート。
- [x] **アンロック処理** (`main.py`): `_next_floor()` で B5F 到達時 `thief` 解放。`_handle_victory()` で Archdemon 撃破時 `mage` 解放 + 即時セーブ。
- [x] **セーブトリガー** (`main.py`): ダンジョン T キー帰還・テレポート帰還・全滅帰還・エンディング帰還・Training 強化直後に `save_data()` を呼び出し。
- [x] **docs/1. 機能要件.md** 更新: セクション「13. 永続化と解放要素」追加。
---

## Phase 11: リファクタリングとモジュール分割（完了）

- [x] **`constants.py` 新規作成**: `STATE_*`, `COL_*`, `TILE_*`, `FRAMES`, `DIR_*`, `VIEW_H`, `MAX_FLOOR`, `INV_MAX`, メニューリスト等の全定数を集約。
- [x] **`logic/map_generator.py` 新規作成**: `Map` クラスと `generate_random` を `data.py` から移動。`TILE_*` は `constants.py` からインポート。
- [x] **`systems/persistence.py` 新規作成**: `serialize_item`, `deserialize_item`, `save_game`, `load_game` を `App` から切り出し、独立した関数として定義。
- [x] **`ui/renderer_3d.py` 新規作成**: `draw_3d_view(wall_at_fn)` を `App` から切り出し、`wall_at` をコールバックとして受け取る形式に変更。
- [x] **`data.py` 更新**: `TILE_*` 定数と `Map` クラスを削除し、データカタログ・クラス定義に限定。
- [x] **`main.py` 更新**: インポートを整理（`constants`, `logic.map_generator`, `systems.persistence`, `ui.renderer_3d` から取得）。定数ブロック・シリアライズメソッド・`draw_3d_view` 本体を削除。行数: 2378 → 2185 行（約 200 行削減）。

## Phase 12: ドキュメント整理・校正（完了）

- [x] **`docs/` ファイル番号の整理**: `2. 仕様詳細.md` → `3. 仕様詳細.md` にリネーム。1:機能要件、2:非機能要件、3:仕様詳細、4:開発タスク の連番構成に統一。
- [x] **`docs/3. 仕様詳細.md` 新規作成**: 旧 `2. 仕様詳細.md` をベースに7カテゴリで再構成。ダメージ計算式・エンチャントテーブル・Renovate/Training費用・画面遷移フローを一元集約。
- [x] **`docs/1. 機能要件.md` 更新**: ゲーム概要セクション追加（企画書より統合）。複雑な計算式・エンチャント詳細テーブルを削除し `3. 仕様詳細.md` へ委譲。開発メモ表記を排除。
- [x] **旧ファイルのアーカイブ**: `2. 仕様詳細_old.md`, `3. 画面遷移.md`, `Delvoker_Specification.md` を `docs/archive/` へ移動。
- [x] **UIレイアウトの最適化**: スキル表示や倉庫画面の重なり解消。（Phase 14.1 で対応: バトルスキル2×2グリッド、倉庫アイテム名 `[:18]` 短縮）
- [x] **BGMの音色改善**: 三角波（Triangle）を基調とした、耳に優しいサウンド設定。（Phase 14.1 で対応: Sound 3/4/5 を Triangle化・音量低減）
- [x] **外部アセット読込の実装**: `pyxel.load("assets.pyxres")` を try-except でラップ、`self.assets_loaded` フラグ管理（Phase 13.1 で実装）。
- [x] **敵モンスターの画像描画化**: `_draw_battle` で `pyxel.blt` による描画に移行。`assets_loaded == False` 時は `pyxel.rect` にフォールバック（Phase 13.1 で実装）。
- [x] **3Dビューのタイルマップ適用検討**: 壁面のテクスチャ化プロトタイプ作成。（Phase 14.3 で対応）

## Phase 13: コア・エクスパンション（グラフィック・戦闘・成長）

### Phase 13.1: グラフィック・アセット化の基盤（完了）
- [x] **アセットロード実装**: `App.__init__` で `pyxel.load("assets.pyxres")` を try-except でラップ。`self.assets_loaded` フラグで描画切替を制御。
- [x] **敵データ拡張**: `EnemyDef` / `Enemy` に `sprite_u` / `sprite_v` フィールドを追加。`data/enemies.json` の全12エントリに 32×32 グリッドのスプライト座標を定義。
- [x] **スプライト描画移行**: `_draw_battle` 内の敵描画を `pyxel.blt` に変更（フォールバック: `pyxel.rect`）。
- [x] **エフェクトシステム導入**: `Effect(x, y, timer=20, type)` クラスを実装。プレイヤー攻撃ヒット時に `"spark"` エフェクトを生成し、`_draw_battle` で 6 点 pset アニメーションを描画。
- [x] **ドキュメント更新**: `docs/3. 仕様詳細.md` に Section 8「グラフィック・アセット仕様」を追記（スプライトレイアウト表・エフェクト仕様含む）。

### Phase 13.2: 戦闘システムの多対多への拡張（完了）
- [x] **データ構造の移行**: `App.enemy` (単体) → `App.enemies` (リスト) に変更。`_start_battle` で `e.enemy_key` を付与。`telegraphing` を `{id: bool}` dict に変更。`target_idx` / `_attack_target` 追加。
- [x] **ターゲット選択UI**: `STATE_BATTLE_TARGET = 21` を `constants.py` に追加。複数体いる場合 Aid → `_upd_battle_target` で ↑↓ 選択・Z決定のカーソル移動を実装。`_draw_battle` に `^` カーソル表示。
- [x] **ターン制御の修正**: `_enemy_turn` を `for acting_enemy in self.enemies` ループに変更。`_run_auto_and_enemy` / `_upd_battle_npc_cmd` / `_handle_victory(msgs, enemy)` を全てリスト対応に修正。
- [x] **描画対応**: `_draw_battle` の敵エリアを `slot_w = SCREEN_W // n` の均等分割レイアウトに変更。1体の場合は従来通りの中央表示。
- [x] **ジョブ固有スキル**: 各ジョブに「挑発」や「範囲攻撃」などのアクティブスキルを定義・実装。（Phase 14.2 で対応: Provoke/Quick Strike/Mana Bolt 実装）

### Phase 13.3: メタプログレッションと図鑑システム（完了）
- [x] **永続化拡張**: セーブデータに `bestiary` (`dict[str, int]`) を追加。`persistence.py` の `save_game` に `bestiary` 引数を追加、`load_game` から `.get("bestiary", {})` で互換ロード。
- [x] **撃破記録ロジック**: `_handle_victory` で `enemy.enemy_key` を参照し `App.bestiary[ekey] += 1` を加算。
- [x] **図鑑UIの実装**: `HOME_MENU` に "Bestiary" 追加。`home_sub = "bestiary"` ブランチで `inv_win` を使用した一覧・詳細パネルを実装（↑↓スクロール、HP/DEF/弱点/耐性/撃破数表示）。

## Phase 14: ポリッシュ & アクティブスキル（完了）

### Phase 14.1: UI & Audio Polish（完了）
- [x] **バトルUI改善**: `_draw_battle` のスキル表示を1行結合から 2×2 グリッド（横60px・縦8px間隔、`cx + col*60, cy+52 + row*8`）に変更。
- [x] **倉庫UI改善**: `_draw_home` Warehouse サブメニューのアイテム名文字数制限を `[:22]` → `[:18]` に短縮、区切り線との衝突を解消。
- [x] **BGM音色改善**: Sound 3/5 の波形を `s`/`p` → `t`（Triangle）に変更。Sound 3 音量 `5`→`3`、Sound 4 音量 `4`→`2`、Sound 5 音量 `6`→`3`。

### Phase 14.2: ジョブ固有スキルの実装（完了）
- [x] **スキルコマンド追加**: `COMMANDS` に `"Skill"` を追加、`STATE_BATTLE_SKILL = 22` を定義。
- [x] **ジョブ固有スキル定義**: `data.py` に `JOB_SKILLS` 辞書を追加。Warrior: `Provoke` (MP:3)、Thief: `Quick Strike` (MP:4)、Mage: `Mana Bolt` (MP:5)。`jobs.json` の Warrior `mp_die` を 0→2 に修正。
- [x] **実行ロジック**: `_upd_battle_skill`・`_execute_active_skill`・`_grant_job_skill` 実装、多対多ターゲット連携（`_pending_skill` 経由で `STATE_BATTLE_TARGET` と統合）。Provoke フック（`_get_enemy_target` / `_enemy_turn`）も実装。

### Phase 14.3: 3D壁面テクスチャ描画（Prototype）（完了）
- [x] **テクスチャ描画**: `ui/renderer_3d.py` で `assets_loaded = True` 時に `pyxel.blt` で壁面描画。
- [x] **バイオーム対応準備**: 階層別 8×8 パターンタイリングのプロトタイプ（`_biome_tile(floor)` / `_blt_tile()`）。

## Phase 15: リバランス & UI修正（完了）

### Phase 15.1: UIレイアウト精密修正（完了）
- [x] **ACTION メニュー幅拡大**: `inv_action_win` の幅を `100` → `120` に変更。
- [x] **SHOP 画面アイテム名短縮**: `_draw_shop` / `_draw_dungeon_shop` でアイテム名を `[:18]` に制限し価格との重なりを防止。
- [x] **価格表示位置調整**: `_draw_shop` 価格 X 座標を `cx + cw - 42`、ダンジョンショップは `px + pw - 46` に変更。

### Phase 15.2: エンカウント・ロジック適正化（完了）
- [x] **歩数カウンター導入**: `App.__init__` に `self.steps_since_encounter = 0` を追加。
- [x] **段階的確率制御**: 戦闘直後 3 歩以内はエンカウント率 0、4歩目から線形上昇（`steps >= 4`、`(n-3)/7`）、10 歩以降で通常確率（`ENCOUNTER_RATE`）。
- [x] **カウンターリセット**: `_start_battle` 冒頭で `steps_since_encounter = 0` にリセット。

### Phase 15.3: 階層別トラップ・扉の再配置（完了）
- [x] **デッドコード除去**: `generate_random` のトラップカウント計算の重複代入を削除。
- [x] **扉数上限バグ修正**: ループ中断条件 `doors_placed >= 2` を `doors_placed >= max_doors` に修正（B3-5F で最大 1 枚が正しく適用される）。
- [x] **鍵チェスト保証強化**: `rooms[1:]` を順番にスキャンして最初の有効タイルに鍵チェストを配置。

## Phase 16: Audio Mute & Shop Sell（完了）

### Phase 16.1: BGMの一時停止（完了）
- [x] `_init_audio` 内の `pyxel.musics[x].set(...)` 3行をコメントアウト（効果音は維持）。

### Phase 16.2: ショップ売却機能の実装（完了）
- [x] `constants.py` に `SHOP_COMMANDS = ["Buy", "Sell"]` を追加。
- [x] `App.__init__` に `self.shop_mode = "buy"` を追加。
- [x] `_set_state(STATE_SHOP)` でモード・インデックスをリセット。
- [x] `_upd_shop`: `KEY_LEFT`/`KEY_RIGHT` でモード切替、Sell モード時の売却ロジック実装（売却価格: `value * 0.3`、装備中は売却不可）。
- [x] `_draw_shop`: `[Buy]`/`[Sell]` タブ表示、Sell モードでインベントリ一覧・装備中アイテム灰色`[E]`表示・売却価格表示。

### Phase 16.3: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` Section 14 に売却機能仕様・BGM 無効化を追記。
- [x] `docs/4. 開発タスク.md` に Phase 15・Phase 16 を追記。

## Phase 17: Porter System Overhaul & UI Polish（完了）

### Phase 17.1: 不具合修正 & 戦闘ロジック適正化（完了）
- [x] **ACTIONメニュー高さ拡大**: `inv_action_win` 高さ `56` → `72`（4項目すべてが枠内に収まる）。
- [x] **街ステータスに防具表示追加**: `_draw_town` の `_status_content` に `Armor: {label}` 行を追加（`cy+46`）。
- [x] **ダンジョンステータスバーに防具表示追加**: `draw_status` に `W:{weapon}  A:{armor}` 行を追加（`STATUS_Y + 40`）、ヘルプテキストを `+52` へ移動。
- [x] **多対多エンカウント強化**: `_start_battle` で階層別に複数敵を生成（B3-5F: 2体30%、B6F+: 2体30%/3体10%）。
- [x] **敵AIターゲット分散**: `_get_enemy_target` の `"normal"` AI を `alive[0]` → `random.choice(alive)` に変更。

### Phase 17.2: ポーターシステム基盤 (Stat & Accessory)（完了）
- [x] **LUK の実装**: `data/jobs.json` に `luk_growth` 追加（warrior:0.3 / thief:0.5 / mage:0.3 / cleric:0.4）。`Status.__init__` に `self.luk = 3`、`_do_level_up` に luk_gain 計算を追加。
- [x] **クリティカル判定**: `_player_attack` に `total_luk / 100` 確率でダメージ×2の `[CRITICAL!]` 演出。
- [x] **逃走成功率補正**: `_try_flee` を `min(0.95, FLEE_RATE + total_luk * 0.01)` に変更。
- [x] **AccessoryItem クラス**: `data.py` に `AccessoryItem(name, luk_bonus, mp_bonus, value)` を追加。
- [x] **アクセサリースロット**: `Status.accessory = None`、`total_luk` / `total_max_mp` プロパティを追加。
- [x] **装飾品アイテム追加**: `items.json` に `lucky_ring` (LUK+5)、`mana_charm` (MP+10)、`rabbits_foot` (LUK+10)、`emergency_kit` (HP+20消費) を追加。
- [x] **セーブ互換性**: `persistence.py` に `AccessoryItem` の serialize/deserialize を追加。
- [x] **UI 更新**: 街ステータス (`status_win` 86px に拡張、LUK・Acc 表示)、ダンジョンステータスバーに `Acc:` 行を追加 (`STATUS_Y+52`)、ヘルプテキストを `+64` へ移動。

### Phase 17.3: 役割の差別化と装備制限（完了）
- [x] **weight_class 導入**: `WeaponItem` / `ArmorItem` に `weight_class` フィールド追加。heavy: long_sword / steel_sword / mithril_sword / steel_plate。
- [x] **ポーター装備制限**: `_do_equip` で `weight_class == "heavy"` の場合に拒否ダイアログを表示。
- [x] **インベントリ拡張**: `INV_MAX = 8` → `12` に変更（Porter の特権）。
- [x] **アクセサリーの NPC 授与**: `_upd_inv_give_npc` に accessory スロット対応を追加。
- [x] **Manage NPC Gear UI**: 「Give to NPC」→「Manage NPC Gear」に変更。NPC の W/A/Acc 現在装備を一覧表示。
- [x] **ショップに新アイテム追加**: `SHOP_KEYS` に `lucky_ring` / `mana_charm` / `rabbits_foot` / `emergency_kit` を追加。

### Phase 17.4: ドキュメント刷新（完了）
- [x] `docs/3. 仕様詳細.md` Section 15 にポーターシステム全仕様（LUK・Acc・weight_class・NPC Gear 管理）を追記。
- [x] `docs/3. 仕様詳細.md` Section 6.2 を「Manage NPC Gear」仕様に更新。
- [x] `docs/4. 開発タスク.md` に Phase 17 完了を記録。

## Phase 18: Porter Refinement（完了）

### Phase 18.1: Porter職の基礎定義（完了）
- [x] **jobs.json にporter追加**: hp_die=8, mp_die=4, str_growth=0.1, def_growth=0.1, agi_growth=0.8, luk_growth=0.7。
- [x] **COMMANDS変更**: `constants.py` の `COMMANDS = ["Fight", ...]` → `["Aid", "Skill", "Flee"]`。
- [x] **Encourage スキル定義**: `data.py` の `JOB_SKILLS["porter"]` に `Skill("Encourage", mp_cost=4, effect_type="encourage", power=10)` を追加。

### Phase 18.2: 戦闘演出とロジックの変更（完了）
- [x] **初期ジョブをporterに固定**: `App.__init__` の `unlocked_jobs = ["porter"]`、`load_data` のフォールバックも `["porter"]` に変更。
- [x] **Aidメッセージ刷新**: `_player_attack` の戦闘ログを「distracts / taunts / harasses / baits / lures / throws debris at / feints against / bluffs at」の表現に変更。
- [x] **Encourageスキル実装**: `_execute_active_skill` に `encourage` 効果（生存全員を `power + total_luk // 2` 回復）を追加。`_upd_battle_skill` で encourage はターゲット選択をスキップ。

### Phase 18.3: ドキュメント同期（完了）
- [x] `docs/1. 機能要件.md` セクション3の戦闘コマンドを Aid/Encourage に更新、セクション12のジョブ解放をporter起点に更新。
- [x] `docs/3. 仕様詳細.md` Section 15.4 にporter初期解放を追記、Section 15.5「Aid演出」・Section 15.6「Encourage計算式」を新規追加。
- [x] `docs/4. 開発タスク.md` に Phase 18 を追加（本記録）。

## Phase 19: Battle Log Viewer & Monster Palette Variants（完了）

### Phase 19.1: ログ履歴基盤（完了）
- [x] `constants.py` に `STATE_LOG_VIEW = 23` を追加し、Battle BGM ゾーンへ登録。
- [x] `App.message_history` を追加し、最大100行のログ履歴を保持。
- [x] `_append_message_history(messages)` を追加し、`_show_msgs` 経由の戦闘メッセージを履歴へ保存。

### Phase 19.2: ログビューアUI（完了）
- [x] 戦闘関連ステート中の `L` キーでログビューアを起動。
- [x] `L` または `X` で起動前の戦闘ステートへ復帰。
- [x] Up/Down で1行、Left/Right でページ単位スクロール。
- [x] `STATE_LOG_VIEW` 中は `update()` 冒頭で専用処理のみ実行し、敵ターン・毒・ポップアップ・エフェクト更新を停止。

### Phase 19.3: モンスター描画バリエーション（完了）
- [x] `Enemy.flip_x` を戦闘生成時にランダム固定し、`pyxel.blt` の負幅で左右反転描画。
- [x] `_enemy_palette_swaps(enemy)` で階層別・ボス別のパレット置換を定義。
- [x] `_draw_enemy_sprite(enemy, x, y)` に `pyxel.pal()` 適用・`pyxel.blt`・リセットを集約。
- [x] `assets_loaded = False` 時の `pyxel.rect` フォールバックを維持。

### Phase 19.4: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` に `STATE_LOG_VIEW`、ログ履歴、ポーズ仕様、パレットスワップ、左右反転を追記。
- [x] `docs/4. 開発タスク.md` に Phase 19 完了を記録。

## Phase 20: Shop Restock, Party Card HUD & Genesis Rarity（完了）

### Phase 20.1: ショップ動的陳列システム（完了）
- [x] `SHOP_STOCK_SIZE = 15`, `SHOP_RESTOCK_STEPS = 100`, `SHOP_RARE_SLOT_RATE = 0.08` を追加。
- [x] `App.shop_stock` / `App.steps_to_restock` を追加し、初期化時と階層移動時に再入荷。
- [x] ダンジョン移動1歩ごとに再入荷カウンターを減算し、0で在庫更新。
- [x] 現在階層ティア中心の在庫生成と、低確率の上位/エンチャント枠を実装。
- [x] Shop Buy UI を15品目の3列×5行グリッド表示に刷新。

### Phase 20.2: パーティカード型HUD（完了）
- [x] `draw_status` を3カード構成に刷新。
- [x] プレイヤーを `[REAR]` / `COL_PEACH` 枠、NPCを `[FRONT]` 枠で表示。
- [x] 各カードにHP/MPバー、武器/防具短縮名、毒/スタンアイコンを表示。
- [x] パーティ空き枠は `-- Empty --` として表示。

### Phase 20.3: Genesis級を含むレアリティ体系（完了）
- [x] `normal` / `rare` / `epic` / `legend` / `genesis` の5段階へ整理。
- [x] `Genesis` は prefix/suffix なしで高基礎性能を持つアイテムとして実装。
- [x] `make_enchanted_weapon` / `make_enchanted_armor` に0.5% Genesis抽選を追加。
- [x] `clone` と `systems/persistence.py` を Genesis / weight_class / ベース性能保持に対応。
- [x] アイテム色分けを `_item_color(item)` に集約し、Genesisは青/白明滅表示。

### Phase 20.4: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` にショップ再入荷、カードHUD、レアリティ仕様を追記。
- [x] `docs/4. 開発タスク.md` に Phase 20 完了を記録。

## Phase 21: Party Item Targeting & Inn Party Recovery（完了）

### Phase 21.1: 宿屋のパーティ回復化（完了）
- [x] Inn利用時に `party.members` の生存メンバー全員のHP/MPを最大まで回復。
- [x] HP=0の戦闘不能メンバーは宿屋では復活せず、Revive管轄を維持。
- [x] 宿泊メッセージをパーティ全体回復の内容へ更新。

### Phase 21.2: アイテム対象選択ステート（完了）
- [x] `STATE_INV_TARGET_SELECT = 24` を追加。
- [x] `pending_use_item` / `inv_target_idx` を追加。
- [x] Potion / Ether / Antidote 等の消費アイテムで対象選択UIへ遷移。
- [x] Mapping Scroll / Grimoire は対象選択なしで従来通り即使用。

### Phase 21.3: 使用可否判定と適用処理（完了）
- [x] `_can_use_item_on(item, target)` を追加し、満タン・状態異常なし・戦闘不能対象の無効使用を防止。
- [x] `_do_use(item, target=None)` へ変更し、HP/MP回復と状態治療を選択対象へ適用。
- [x] 回復ポップアップを対象パーティカード位置へ出すよう調整。

### Phase 21.4: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` に宿屋、対象選択、使用可否判定、`_do_use(item, target)` を追記。
- [x] `docs/4. 開発タスク.md` に Phase 21 完了を記録。

## Phase 22: Party Size 4 & Monster Sprite Fix（完了）

### Phase 22.1: パーティ最大人数の4名化（完了）
- [x] `Party.MAX_SIZE` を3から4へ変更し、Player + NPC3 の編成を許可。
- [x] パーティ人数固定値を検索し、HUDやポップアップ座標を `Party.MAX_SIZE` 基準へ更新。
- [x] 敵AIターゲット、宿屋、Revive、NPC装備管理、アイテム対象選択が `party.members` / `party.alive` 基準で動作することを確認。

### Phase 22.2: 4カードHUDレイアウト（完了）
- [x] `draw_status` を4カード構成へ変更。
- [x] 名前・装備名の短縮幅を4分割カードに合わせて調整。
- [x] 空スロット表示を `Empty` に短縮し、4枚カード内に収まるよう調整。
- [x] 戦闘中のNPC HP表示を3名分が横に収まるよう圧縮。

### Phase 22.3: モンスター画像表示診断（完了）
- [x] `assets.pyxres` ロード失敗時にコンソール警告を出すよう変更。
- [x] `_draw_enemy_sprite` 内でアセット未ロード・座標不正・`blt` 失敗時の診断ログを一度だけ出すよう追加。
- [x] `_draw_enemy_sprite` がフォールバック矩形まで描画する責務を持つよう整理。
- [x] `try/finally` で `pyxel.pal()` リセットを保証。

### Phase 22.4: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` のパーティ人数、Party仕様、4カードHUD、敵スプライト診断仕様を更新。
- [x] `docs/4. 開発タスク.md` に Phase 22 完了を記録。

## Phase 23: Assets Pyxres Creation（完了）

### Phase 23.1: `assets.pyxres` 制作（完了）
- [x] Image Bank 0 に全12体の32×32モンスタースプライトを指定座標で配置。
- [x] 透過色0を背景として使用し、黒背景でも読めるよう明部輪郭を追加。
- [x] Image Bank 0 の `(0,64)`, `(8,64)`, `(16,64)` に8×8壁テクスチャを配置。

### Phase 23.2: 壁テクスチャ参照座標の同期（完了）
- [x] `ui/renderer_3d.py` の `_BIOME_TILES` を `[(0,64), (8,64), (16,64)]` へ変更。
- [x] `assets_loaded = False` 時の単色フォールバックを維持。

### Phase 23.3: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` の敵スプライト制作方針と壁テクスチャ座標を更新。
- [x] `docs/4. 開発タスク.md` に Phase 23 完了を記録。
- [x] 使用した実装指示書を `docs/archive/` へ移動。

## Phase 24: Dungeon NPC Sprites, Name Input, and Rich Backgrounds（完了）

### Phase 24.1: ダンジョンNPCスプライト化（完了）
- [x] `NPC.draw()` に `assets_loaded` 引数を追加。
- [x] `ENEMY_CATALOG` の `sprite_u` / `sprite_v` を参照し、探索NPCをモンスタースプライトで描画。
- [x] 距離別サイズに合わせた最近傍スケール描画を追加。
- [x] `assets.pyxres` 未ロード時や座標不備時は単色矩形フォールバックを維持。

### Phase 24.2: 名前入力とPorter固定開始（完了）
- [x] `STATE_NAME_INPUT = 25` を追加。
- [x] New Game → 名前入力 → Porter固定開始 → Town のフローへ変更。
- [x] Continue は名前入力・職業選択を挟まず、保存データ読込後に Town へ遷移。
- [x] プレイヤー名をセーブデータへ保存し、旧セーブでは `HERO` にフォールバック。

### Phase 24.3: 背景画像Bank導入（完了）
- [x] `assets.pyxres` の Bank 1 にタイトル背景、Bank 2 にエンディング背景を追加。
- [x] `_draw_title()` と `_draw_ending()` で背景Bankを描画。
- [x] `assets_loaded = False` 時の黒背景フォールバックを維持。

### Phase 24.4: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` に画面遷移、探索NPCスプライト、背景Bank仕様を追記。
- [x] `docs/4. 開発タスク.md` に Phase 24 完了を記録。
- [x] 使用した実装指示書を `docs/archive/` へ移動。

## Phase 25: Image 2.0 Asset Import（完了）

### Phase 25.1: 生成素材の受け入れ（完了）
- [x] `assets/source/image2/monsters/` のモンスターPNGを確認。
- [x] `assets/source/image2/walls/` の壁PNGを確認。
- [x] `assets/source/image2/backgrounds/` のタイトル・エンディング背景PNGを確認。

### Phase 25.2: 変換スクリプト整備（完了）
- [x] `tools/convert_image2_assets.py` を追加。
- [x] モンスターPNGを背景色透過扱いで32×32へ縮小し、Bank 0の指定座標へ配置。
- [x] 壁PNGを8×8へ縮小し、Bank 0の `(0,64)`, `(8,64)`, `(16,64)` へ配置。
- [x] 背景PNGを256×256へ縮小し、Bank 1/2へ配置。

### Phase 25.3: `assets.pyxres` 更新（完了）
- [x] Image 2.0 素材を `assets.pyxres` へ取り込み。
- [x] Bank 0/1/2 の代表ピクセルを読み戻し確認。
- [x] Python構文チェックを実施。

### Phase 25.4: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` に Image 2.0 素材取り込み仕様を追記。
- [x] `docs/4. 開発タスク.md` に Phase 25 完了を記録。
- [x] 使用した実装指示書を `docs/archive/` へ移動。

## Phase 26: Asset Replacement, Battle Centering, NPC Growth and Recruitment（完了）

指示書: `docs/archive/plan_20260512_asset_replacement_battle_layout_npc_growth_recruitment.md`

### Phase 26.1: 再作成モンスター画像の差し替え（完了）
- [x] 再作成済みモンスターPNGを `assets/source/image2/monsters/` から読み込み、既存ファイル名を維持する。
- [x] 暗く輪郭が不明瞭なモンスター、縦に間延びしたモンスターを差し替え対象として整理する。
- [x] `tools/convert_image2_assets.py --monsters` で差し替え対象のみをBank 0既存座標へ上書きする。
- [x] 差し替え対象外の壁テクスチャ、背景を不用意に上書きしない。

### Phase 26.2: モンスター変換品質の補正（完了）
- [x] クロマキー/透明背景をPyxel色 index 0 として扱う。
- [x] 本体バウンディングボックスを抽出し、正方形キャンバスへ中央配置してから32x32へ縮小する。
- [x] 変換後に輪郭・顔・角・翼・武器などの主要パーツが可視色として残るよう、比率維持の取り込みに更新する。
- [x] Bank 0の差し替え座標を `--monsters` 実行ログで確認する。

### Phase 26.3: 戦闘画面の単体モンスター中央寄せ（完了）
- [x] `_draw_battle()` で `len(self.enemies) == 1` の場合、敵中心を `SCREEN_W // 2` に明示する。
- [x] 名前、スプライト、HPバー、ターゲットカーソル、ポップアップが同じ中心線に揃うようにする。
- [x] 複数敵では既存の均等スロット配置を維持する。
- [x] `enemy.flip_x` の負幅描画は既存仕様を維持し、スプライト側の中央配置で見た目を補正する。

### Phase 26.4: NPC経験値配布とレベルアップ修正（完了）
- [x] `_handle_victory()` のEXP配布を、プレイヤーのみから生存パーティメンバー全員へ拡張する。
- [x] HP 0 の戦闘不能NPCにはEXPを配布しない。
- [x] `NPCMember` でも `Status.gain_exp()` を使ってレベルアップするようにする。
- [x] NPCレベルアップ時にASCIIログを追加し、プレイヤー用 `level_up_gains` と混同しない。

### Phase 26.5: 探索NPCの仲間化導線（完了）
- [x] 探索NPC接触時に、即戦闘だけでなく仲間化可能NPCの分岐を追加する。
- [x] 仲間化可能/敵対NPCを区別する `recruitable` フラグと `job_key` / `personality` を追加する。
- [x] `Recruit {name}?` 相当のASCII選択UIを実装する。
- [x] Join / Fight / Leave の各分岐を実装する。
- [x] パーティ満員時は加入不可メッセージを表示し、戦闘または離脱へ進める。
- [x] 加入成功時は `NPCMember` を生成して `party.add()` し、探索NPCをマップから除去する。
- [x] 加入NPCの初期レベル・HP/MPを現在階層に応じて調整する。

### Phase 26.6: 仲間化NPCの保存・既存機能連携（完了）
- [x] 仲間化NPCがGuild昇格、NPC装備管理、Revive、宿屋、アイテム対象選択、戦闘AIで既存NPCと同様に扱われる構造にする。
- [x] `systems/persistence.py` にパーティNPCの保存/復元を追加する。
- [x] 旧セーブでは既存の初期NPC加入フォールバックを維持する。
- [x] セーブ/ロード後も仲間化NPCの名前、ジョブ、性格、レベル、EXP、装備、固有化状態が維持されるようにする。

### Phase 26.7: ドキュメント同期（完了）
- [x] `docs/3. 仕様詳細.md` にモンスター差し替え品質、戦闘中央寄せ、NPC EXP、仲間化、保存仕様を追記する。
- [x] `docs/4. 開発タスク.md` の Phase 26 チェックを実装結果に合わせて更新する。
- [x] 実装・検証完了後、使用した指示書を `docs/archive/` へ移動する。

---

## Phase 27: Content Variety Expansion（完了）

指示書: `docs/archive/plan_20260513_content_variety_expansion_tasks.md`

### Phase 27.1: 追加コンテンツ設計（完了）
- [x] 既存の `data/enemies.json`、`data/items.json`、`data/enchants.json` を確認し、現行のHP、DEF、武器ダイス、報酬、価格レンジを整理する。
- [x] 追加モンスターは B1F-B3F / B4F-B6F / B7F-B10F に各2体、合計6体を基本単位として設計する。
- [x] 追加武器は各ティア2種、合計6種を基本単位として設計する。
- [x] 追加防具は各ティア2種、合計6種を基本単位として設計する。
- [x] 既存属性 `fire` / `ice` / `poison` / `holy` とAIタイプ `normal` / `ranged` / `support` を優先利用し、新システム追加を最小限にする。
- [x] ゲーム内表示名はASCIIのみとし、UI短縮表示でも判別できる長さに抑える。

### Phase 27.2: 低階層モンスター追加（B1F-B3F）（完了）
- [x] `kobold` を追加する。Goblinより速い軽量敵として、AGI型・軽装備の価値を序盤から見せる。
- [x] `kobold` のAIは `ranged` または `normal` とし、既存序盤敵より行動の違いが分かる性能にする。
- [x] `ooze` を追加する。低攻撃・高HP・poison耐性の耐久敵として、毒属性だけに頼る戦い方への軽い対策にする。
- [x] `ooze` のAIは `normal` とし、序盤で理不尽にならない攻撃力に調整する。
- [x] B1F-B3Fの `_encounter_pool(floor)` に、既存 Slime / Bat / Goblin / Skeleton との出現比率を崩さない形で追加する。
- [x] 低階層のEXP/Goldは既存敵の `8-20 EXP`、`3-15 Gold` を基準に調整する。

### Phase 27.3: 中階層モンスター追加（B4F-B6F）（完了）
- [x] `cultist` を追加する。support AIでHPが低い味方を狙う人型敵として、複数戦でのターゲット優先度を作る。
- [x] `cultist` には `holy` 弱点を設定し、Holy系攻撃の価値を上げる。
- [x] `ice_hound` を追加する。fire弱点・ice耐性の属性チュートリアル強化敵として設計する。
- [x] `ice_hound` のAIは `ranged` とし、中階層の機動力がある敵として差別化する。
- [x] B4F-B6Fの `_encounter_pool(floor)` に追加し、Wraith / Golem / Mimic / Wyvern との難度差を調整する。
- [x] 中階層のEXP/Goldは既存敵の `30-70 EXP`、`20-50 Gold` を基準に調整する。

### Phase 27.4: 高階層モンスター追加（B7F-B10F）（完了）
- [x] `dark_knight` を追加する。高DEF・holy弱点・stun付与の重装敵として、holy攻撃と高火力武器の価値を上げる。
- [x] `dark_knight` のAIは `normal` または `support` とし、終盤通常敵として戦術的な圧を持たせる。
- [x] `lich` を追加する。holy弱点・fire/ice耐性・telegraph持ちの魔術師敵として、終盤の準ボス級通常敵にする。
- [x] `lich` のAIは `support` とし、低HPメンバー狙いと予兆攻撃で緊張感を作る。
- [x] B7F-B10Fの `_encounter_pool(floor)` に追加し、Archdemon前の終盤プールを厚くする。
- [x] 高階層のEXP/Goldは既存敵とボスの間に収め、通常戦の報酬過多を避ける。

### Phase 27.5: 武器バリエーション追加（完了）
- [x] `hand_axe`（Hand Axe）を追加する。最大値寄りの序盤武器として設計する。
- [x] `spear`（Spear）を追加する。軽量で扱いやすい序盤武器として設計する。
- [x] `war_hammer`（War Hammer）を追加する。高DEF敵に刺さる固定値寄りの中階層武器として設計する。
- [x] `rune_staff`（Rune Staff）を追加する。Mage向けの雰囲気を持つ軽量中階層武器として設計する。
- [x] `dragon_slayer`（Dragon Slayer）を追加する。高価格・高火力の終盤重武器として設計する。
- [x] `shadow_blade`（Shadow Blade）を追加する。軽量高額の終盤レア武器として設計する。
- [x] 追加武器はすべて `make_enchanted_weapon()` の既存抽選に乗る基礎武器として定義する。
- [x] ダイス性能は Old Dagger < Short Sword < Long Sword < Steel Sword < Mithril Sword の段階を壊さない範囲で調整する。

### Phase 27.6: 防具バリエーション追加（完了）
- [x] `padded_armor`（Padded Armor）を追加する。安価なDEF+1装備として設計する。
- [x] `hunter_cloak`（Hunter Cloak）を追加する。軽量でThief/NPC向けのDEF+2装備として設計する。
- [x] `scale_mail`（Scale Mail）を追加する。Chain MailとSteel Plateの中間装備として設計する。
- [x] `mage_robe`（Mage Robe）を追加する。低DEFだが軽量・魔法職向けの装備候補として設計する。
- [x] `dragon_mail`（Dragon Mail）を追加する。高DEFかつfire耐性候補の終盤防具として設計する。
- [x] `aegis_plate`（Aegis Plate）を追加する。高価格・高DEFの終盤重防具として設計する。
- [x] 追加防具はすべて `make_enchanted_armor()` の既存抽選に乗る基礎防具として定義する。
- [x] 防具の `weight_class` は既存の装備制限・表示仕様と矛盾しないように設定する。

### Phase 27.7: 部位破壊と特殊ドロップ追加（完了）
- [x] 通常敵のうち2-3体にだけ部位破壊を追加し、通常戦のテンポを壊さない範囲に留める。
- [x] `wyvern` に Wing 部位を追加し、破壊時に `wing_boots` などの軽量防具またはアクセサリー候補をドロップさせる。
- [x] `golem` に Core 部位を追加し、破壊時に `stone_core` などの高DEF素材装備候補をドロップさせる。
- [x] `dark_knight` に Shield 部位を追加し、破壊時に `black_shield` などの防具候補をドロップさせる。
- [x] 既存の部位破壊ドロップ率20%を基本として流用する。
- [x] 特殊ドロップ用アイテムは `data/items.json` に追加し、通常ドロッププールには入れすぎない。
- [x] 部位名、ログ、アイテム名はASCIIのみとする。

### Phase 27.8: ドロップ・ショップ・経済バランス調整（完了）
- [x] `_drop_pools(floor)` に追加武器・防具を階層別に配分する。
- [x] ショップ在庫候補にも低-中階層装備を中心に追加し、高階層装備はドロップ・宝箱寄りにする。
- [x] 低階層装備の価格は20-120G程度を基準にする。
- [x] 中階層装備の価格は150-400G程度を基準にする。
- [x] 高階層装備の価格は500G以上を基準にする。
- [x] レアリティ抽選、Genesis抽選、売却価格30%の既存仕様を壊さない。
- [x] 宝箱・敵ドロップ・ショップのどれで入手できるかを装備ごとに整理する。

### Phase 27.9: スプライト・Bestiary・表示連携（完了）
- [x] 新規モンスターに `sprite_u` / `sprite_v` を割り当てる。
- [x] 既存Bank 0の32x32モンスターグリッドと衝突しない座標を使う。
- [x] 新規スプライトがまだない場合は矩形フォールバックでも判別できるよう、名前と頭文字表示を確認する。
- [x] Bestiaryに追加モンスターが撃破後に表示されることを確認する。
- [x] 敵名・装備名が戦闘画面、インベントリ、ショップ、ログでUIからはみ出さないよう確認する。

### Phase 27.10: ドキュメント同期と検証（完了）
- [x] `docs/3. 仕様詳細.md` に追加モンスター、追加装備、部位破壊ドロップ、階層別プールの仕様を追記する。
- [x] `docs/4. 開発タスク.md` の Phase 27 チェックリストを実装結果に合わせて更新する。
- [x] 実装・検証完了後、使用した指示書を `docs/archive/` へ移動する。

---

## Phase 28: New Monster Image 2.0 Import（完了）

指示書: `docs/archive/plan_20260513_phase28_new_monster_image2_import.md`

### Phase 28.1: Source Image Check（完了）
- [x] `assets/source/image2/monsters/` に新規6体のPNGが存在することを確認する。
- [x] `monster_kobold.png`, `monster_ooze.png`, `monster_cultist.png`, `monster_ice_hound.png`, `monster_dark_knight.png`, `monster_lich.png` のファイル名を確認する。
- [x] `data/enemies.json` の敵キーとPNGファイル名の対応を確認する。

### Phase 28.2: Conversion Script Update（完了）
- [x] `tools/convert_image2_assets.py` の `MONSTERS` に新規6体を追加する。
- [x] `data/enemies.json` の `sprite_u` / `sprite_v` と一致する座標を指定する。
- [x] 既存モンスター12体の座標は変更しない。

### Phase 28.3: Monster-Only Import（完了）
- [x] `tools/convert_image2_assets.py --monsters` でモンスターのみ `assets.pyxres` に取り込む。
- [x] 壁テクスチャ、タイトル背景、エンディング背景は今回の取り込み対象外にする。
- [x] 実行ログで新規6体が指定座標へ取り込まれたことを確認する。

### Phase 28.4: Readback Verification（完了）
- [x] `assets.pyxres` Bank 0 の新規6座標を読み戻す。
- [x] 各32x32タイルに非0ピクセルが存在することを確認する。
- [x] 各タイルの使用色数が極端に少なすぎないことを確認する。

### Phase 28.5: Runtime Display Check（完了）
- [x] 新規6体の `sprite_u` / `sprite_v` が `data/enemies.json` と変換スクリプトで一致していることを確認する。
- [x] 新規スプライト未配置時の矩形フォールバックではなく、Bank 0の実スプライトを参照できる状態にする。
- [x] 探索NPC描画でも新規敵キーに対応した画像を参照できる状態にする。

### Phase 28.6: Documentation Sync（完了）
- [x] `docs/3. 仕様詳細.md` の敵スプライト表に新規6体の座標を追記する。
- [x] `docs/4. 開発タスク.md` に Phase 28 完了を記録する。
- [x] 実装・検証完了後、使用した指示書を `docs/archive/` へ移動する。

---

## Phase 29: Dungeon Perspective Surface Import（完了）

指示書: `docs/archive/plan_20260514_image2_dungeon_perspective_surfaces.md`

### Phase 29.1: Source Image Check（完了）
- [x] `assets/source/image2/surfaces/` に15枚のPNGが存在することを確認する。
- [x] ceiling / side wall / front wall / floor / far の5面をB1F-B2F / B3F-B4F / B5F-B10Fの3階層ティア分そろえる。

### Phase 29.2: Conversion Script Update（完了）
- [x] `tools/convert_image2_assets.py` に `SURFACES` 定義を追加する。
- [x] `--surfaces` オプションでsurface素材のみをBank 0へ取り込めるようにする。
- [x] Bank 0 の `(24,64)` から `(136,64)` へ15枚の8x8タイルを配置する。

### Phase 29.3: 3D Renderer Update（完了）
- [x] `ui/renderer_3d.py` で ceiling / side wall / front wall / floor / far の参照タイルを分ける。
- [x] 左側の横壁は右側横壁素材を左右反転して描画する。
- [x] 正面壁は距離段階に関係なくFront Wall素材を使い、Far Endは最奥背景面として使う。

### Phase 29.4: Import and Verification（完了）
- [x] `tools/convert_image2_assets.py --surfaces` で15枚を `assets.pyxres` に取り込む。
- [x] Bank 0 の各surfaceタイルに非0ピクセルが存在することを確認する。
- [x] `draw_3d_view(..., assets_loaded=True, dungeon_floor=5)` がクラッシュしないことを確認する。

### Phase 29.5: Documentation Sync（完了）
- [x] `docs/3. 仕様詳細.md` にsurface取り込みと3Dビュー面別タイル仕様を反映する。
- [x] `docs/4. 開発タスク.md` に Phase 29 完了を記録する。
- [x] 実装・検証完了後、使用した指示書を `docs/archive/` へ移動する。

---

## Phase 30: Full Regression Test Play and Bug Fix（完了）

優先Issue: Issue #04（全体テストプレイとバグフィックス）

### Phase 30.1: テスト指示書作成（完了）
- [x] `docs/instructions/plan_20260515_full_regression_test_play.md` を作成する。
- [x] 既存仕様 `docs/1. 機能要件.md` / `docs/3. 仕様詳細.md` と照合し、検証範囲を確定する。
- [x] 発見した不具合の記録形式（再現手順、期待結果、実結果、修正方針）を指示書に定義する。

### Phase 30.2: 起動・セーブ互換・基本遷移検証（完了）
- [x] New Game から名前入力、Town到達までクラッシュしないことを確認する。
- [x] Continue / 既存セーブ読み込みでクラッシュしないことを確認する。
- [x] Town / Home / Guild / Shop / Dungeon / Battle / Log View への主要遷移を確認する。
- [x] `assets.pyxres` 読み込み成功時と失敗時のフォールバック挙動を確認する。

### Phase 30.3: 探索・3Dビュー・ダンジョン表示検証（完了）
- [x] B1F-B2F / B3F-B4F / B5F-B10F の3Dビュー面素材が階層ごとに切り替わることを確認する。
- [x] ceiling / side wall / front wall / floor / far の描画パスがクラッシュしないことを確認する。
- [x] 深層B5F以降で3Dビューが描画不能にならないことを確認する。
- [x] 鍵扉、宝箱、罠、階段、NPC表示を含む探索画面の描画スモークを確認する。

### Phase 30.4: 戦闘・スキル・部位破壊検証（完了）
- [x] 通常戦闘でプレイヤー/NPC/敵のターン進行が止まらないことを確認する。
- [x] Aid / Provoke / Quick Strike / Mana Bolt など主要コマンドの実装経路を確認する。
- [x] 複数敵、複数味方、対象選択、逃走禁止、勝利フローを確認する。
- [x] 部位ターゲット表示、勝利時Bestiary登録、特殊ドロップ参照の整合性を確認する。

### Phase 30.5: アイテム・ショップ・成長検証（完了）
- [x] アイテム使用、対象選択、装備変更、NPCへの受け渡しの描画スモークを確認する。
- [x] Shopの購入/売却画面、Restock、価格表示、所持金表示を確認する。
- [x] 宿屋、Revive、レベルアップ後のステータス割り振り経路を確認する。
- [x] 追加装備、Genesis品、呪い/耐性、保存シリアライズ参照の整合性を確認する。

### Phase 30.6: UIはみ出し・可読性検証（完了）
- [x] Party HUD、Battle HUD、Inventory、Shop、Guild、Bestiary、Log Viewの描画スモークを確認する。
- [x] 4人パーティ、複数敵、長い装備名/敵名でクラッシュしないことを確認する。
- [x] 3Dビュー更新後のsurface描画、HUD、NPC描画の共存を確認する。
- [x] ゲーム内表示文字列のASCII制約とPyxel色番号0-15制約を静的チェックする。

### Phase 30.7: 修正・再検証・ドキュメント同期（完了）
- [x] Critical / High のクラッシュ、進行不能、セーブ破壊不具合が検出されないことを確認する。
- [x] 構文チェック、surface取り込み、3D描画、自動回帰スモークを再検証する。
- [x] `docs/3. 仕様詳細.md` の3Dビューsurfaceタイリング仕様を同期する。
- [x] Phase 30完了後、`docs/4. 開発タスク.md` を完了状態へ更新し、指示書を `docs/archive/` へ移動する。

検証コマンド:
- `python -B -m py_compile constants.py data.py logic/map_generator.py main.py npc.py systems/persistence.py tools/convert_image2_assets.py ui/renderer_3d.py window.py`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B tools/convert_image2_assets.py --surfaces`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... draw_3d_view smoke ..."`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B` による29項目の自動回帰スモーク
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp timeout 3 python -B main.py`（タイムアウト終了は想定通り、起動クラッシュなし）

---

## Phase 31: 3D Surface Readability Fix（完了）

指示書: `docs/archive/plan_20260515_surface_readability_fix.md`

優先Issue: B1F-B2Fの3Dビューsurfaceが赤みがかり、ちらつく千鳥格子状に見える問題の修正。

### Phase 31.1: 調査・方針確定（完了）
- [x] `docs/instructions/plan_20260515_surface_readability_fix.md` を作成する。
- [x] 3Dビューの奥行きジオメトリは維持し、surface素材の配色とパターン密度を修正対象にする。

### Phase 31.2: Surface素材再生成（完了）
- [x] B1F-B2Fから赤系・ピンク系を除外し、低コントラストの石壁色へ変更する。
- [x] B3F-B4F / B5F-B10Fも同じ低ノイズ方針で再生成する。
- [x] 1px交互の市松・千鳥格子を主模様にしない。

### Phase 31.3: 取り込み・検証・同期（完了）
- [x] `tools/convert_image2_assets.py --surfaces` で `assets.pyxres` へ再取り込みする。
- [x] B1F-B2F surfaceタイルに赤系色 `8,14,15` が含まれないことを確認する。
- [x] `draw_3d_view(..., assets_loaded=True, dungeon_floor=1/3/5)` がクラッシュしないことを確認する。
- [x] `docs/3. 仕様詳細.md` にSurface可読性基準を追記する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B tools/convert_image2_assets.py --surfaces`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... surface tile color and draw_3d_view smoke ..."`

---

## Phase 32: Simplify 3D Surface Tiles（完了）

指示書: `docs/archive/plan_20260515_surface_simplify_tiles.md`

優先Issue: Phase 31後もB1F-B2Fが青く、8x8反復による千鳥格子・ちらつき感が残る問題の修正。

### Phase 32.1: 方針確定（完了）
- [x] 細かい石目やドット装飾を廃止し、surfaceタイルをほぼ単色面にする。
- [x] 3Dビューの奥行きジオメトリは維持し、見た目の問題を素材の情報量削減で解消する。

### Phase 32.2: Surface素材単純化（完了）
- [x] B1F-B2Fは灰色 `13` を主色にし、青系・赤系を除外する。
- [x] 横壁は完全単色にし、縦ストリップ描画でちらつく細かい模様を入れない。
- [x] B3F-B4F / B5F-B10Fも同じ単純化方針で更新する。

### Phase 32.3: 取り込み・検証・同期（完了）
- [x] `tools/convert_image2_assets.py --surfaces` で `assets.pyxres` へ再取り込みする。
- [x] B1F-B2F surfaceタイルが `0,13` または `13` のみで構成されることを確認する。
- [x] `draw_3d_view(..., assets_loaded=True, dungeon_floor=1/3/5)` がクラッシュしないことを確認する。
- [x] `docs/3. 仕様詳細.md` にSurface単純化基準を追記する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B tools/convert_image2_assets.py --surfaces`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... simplified surface color and draw_3d_view smoke ..."`

---

## Phase 33: Directional Surface Line Patterns（完了）

指示書: `docs/archive/plan_20260515_surface_remove_lines.md`

優先Issue: surfaceの線方向を面ごとに分け、左右壁・天井・床の区別を改善する。

### Phase 33.1: 方針修正（完了）
- [x] 線だけのタイルではなく、模様の中で左右壁は縦方向、天井と床は横方向を強調する方針へ変更する。
- [x] 市松・千鳥格子・斜め格子に見える交互配置は避ける。
- [x] 8x8では線だけが強く出るため、surfaceタイルを16x16へ拡張する。

### Phase 33.2: Surface素材再生成（完了）
- [x] B1F-B2Fは青系・赤系を避け、天井/床を横方向アクセント、横壁/正面壁を縦方向アクセントのある石目模様にする。
- [x] B3F-B4F / B5F-B10Fも同じ線方向ルールで更新する。
- [x] 奥面は暗色中心の控えめな模様にする。

### Phase 33.3: 取り込み・検証・同期（完了）
- [x] `tools/convert_image2_assets.py --surfaces` で `assets.pyxres` へ再取り込みする。
- [x] Bank 0 の `V=128` 行に16x16 surfaceタイルとして取り込まれることを確認する。
- [x] B1F-B2Fプレビューで、天井/床は横方向、横壁/正面壁は縦方向が模様内で強調されることを確認する。
- [x] `draw_3d_view(..., assets_loaded=True, dungeon_floor=1/3/5)` がクラッシュしないことを確認する。
- [x] `docs/3. 仕様詳細.md` にSurface線方向ルールを追記する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B tools/convert_image2_assets.py --surfaces`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... directional surface tile and draw_3d_view smoke ..."`

---

## Phase 34: Degrid Surface Readability Fix（完了）

指示書: `docs/archive/plan_20260515_surface_degrid_readability.md`

優先Issue: 黒い連続線により、灰色の四角いパネルが浮いて見える3D surface表示の修正。

### Phase 34.1: 原因特定（完了）
- [x] 黒い連続線が格子・枠として反復され、浮いたパネルのように見えることを確認する。
- [x] PNG読み込み経由では16x16 surfaceの未サンプル領域が黒く混入することを確認する。

### Phase 34.2: Surface取り込み修正（完了）
- [x] `tools/convert_image2_assets.py` に16x16 surfaceパターン定義を追加する。
- [x] surfaceはPNG読み込みではなく、定義済みパターンをBank 0へ直接書き込む。
- [x] B1F-B2Fの可視面から黒 `0` を除外し、短い擦れ模様に変更する。

### Phase 34.3: 検証・同期（完了）
- [x] `tools/convert_image2_assets.py --surfaces` で `assets.pyxres` へ再取り込みする。
- [x] B1F-B2FのCeiling / Side Wall / Front Wall / Floorが `7,13` のみで構成されることを確認する。
- [x] B1F-B2F / B3F-B4F / B5F-B10F の3Dプレビューを保存する。
- [x] `draw_3d_view(..., assets_loaded=True, dungeon_floor=1/3/5)` がクラッシュしないことを確認する。
- [x] `docs/3. 仕様詳細.md` にSurface格子回避基準を追記する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B tools/convert_image2_assets.py --surfaces`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... degrid surface color and draw_3d_view smoke ..."`

---

## Phase 35: Surface Depth Shading（完了）

指示書: `docs/archive/plan_20260515_surface_depth_shading.md`

優先Issue: 3Dビューの遠近感と空間表現が弱く、空間の色合いをより濃くしたい問題の修正。

結果: Phase 35の濃色化と黒フレーム強調は見た目を悪化させたため、Phase 36で撤回。

### Phase 35.1: 色調整（完了）
- [x] B1F-B2Fの可視surfaceから白 `7` を除外し、`1,13` の濃い灰色基調へ変更する。
- [x] B3F-B4Fは `1,5`、B5F-B10Fは `1,2` を基本とし、中層・深層の色分けを維持する。

### Phase 35.2: 奥行き表現（完了）
- [x] ditherによる暗色オーバーレイは市松ノイズになるため採用しない。
- [x] surface描画時の奥行きフレームと面境界線を黒 `0` にして、空間の箱形を読みやすくする。

### Phase 35.3: 検証・同期（完了）
- [x] `tools/convert_image2_assets.py --surfaces` で `assets.pyxres` へ再取り込みする。
- [x] B1F-B2F / B3F-B4F / B5F-B10F の3Dプレビューを保存する。
- [x] `draw_3d_view(..., assets_loaded=True, dungeon_floor=1/3/5)` がクラッシュしないことを確認する。
- [x] `docs/3. 仕様詳細.md` にSurface奥行き表現を追記する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B tools/convert_image2_assets.py --surfaces`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... depth surface color and draw_3d_view smoke ..."`

---

## Phase 36: Revert Phase 35 Surface Depth Changes（完了）

指示書: `docs/archive/plan_20260515_surface_revert_phase35.md`

優先Issue: Phase 35の見た目悪化を撤回し、Phase 34相当の表示へ戻す。

### Phase 36.1: 撤回対象の整理（完了）
- [x] B1F-B2Fの `1,13` 濃色化を撤回する。
- [x] surface描画時の黒 `0` フレーム強調を撤回する。
- [x] dither暗色オーバーレイは引き続き不採用とする。

### Phase 36.2: Surface復元（完了）
- [x] B1F-B2Fの可視surfaceを `7,13` に戻す。
- [x] B3F-B4Fを `3,5`、B5F-B10Fを `1,2` に戻す。
- [x] `tools/convert_image2_assets.py --surfaces` で `assets.pyxres` へ再取り込みする。

### Phase 36.3: 検証・同期（完了）
- [x] B1F-B2F / B3F-B4F / B5F-B10F の3Dプレビューを保存する。
- [x] `draw_3d_view(..., assets_loaded=True, dungeon_floor=1/3/5)` がクラッシュしないことを確認する。
- [x] `docs/3. 仕様詳細.md` にPhase 35撤回とPhase 36基準を反映する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B tools/convert_image2_assets.py --surfaces`
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... phase36 preview and draw_3d_view smoke ..."`

---

## Phase 37: Disable 3D Surface Textures（完了）

指示書: `docs/archive/plan_20260515_disable_surface_textures.md`

優先Issue: surfaceタイル調整が収束せず、タイル反復由来の破綻を止める。

### Phase 37.1: 方針転換（完了）
- [x] surfaceテクスチャによる見た目調整を中断する。
- [x] ゲーム画面としての可読性を優先し、単色ポリゴン描画へ戻す。

### Phase 37.2: 実装（完了）
- [x] `ui/renderer_3d.py` に `USE_SURFACE_TEXTURES = False` を追加する。
- [x] `assets_loaded=True` でもsurfaceタイルを描画せず、既存の単色遠近ポリゴン描画を使う。
- [x] surface素材と変換定義は将来再検討用に残す。

### Phase 37.3: 検証・同期（完了）
- [x] `draw_3d_view(..., assets_loaded=True/False, dungeon_floor=1/3/5)` がクラッシュしないことを確認する。
- [x] `assets_loaded=True` と `assets_loaded=False` のプレビューを保存する。
- [x] `docs/3. 仕様詳細.md` にSurface無効化方針を反映する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... phase37 assets/fallback preview and draw_3d_view smoke ..."`

---

## Phase 38: Solid Dungeon Palette（完了）

指示書: `docs/archive/plan_20260515_solid_dungeon_palette.md`

優先Issue: surface texture無効化後の単色3Dビューで、緑系の壁色が不自然に見える問題の修正。

結果: 実画面で不自然だったため、Phase 39で撤回。

### Phase 38.1: 配色修正（完了）
- [x] `WALL_COLS` から緑系 `COL_GREEN` / `COL_DARK_GREEN` を除外する。
- [x] `WALL_COLS` を `[13, 4, 4, 0]` に変更し、グレー/茶/黒の単色ダンジョン配色にする。
- [x] 単色ポリゴンの輪郭線を黒 `0` にして、青みを抜く。

### Phase 38.2: 検証・同期（完了）
- [x] `assets_loaded=True/False`、`dungeon_floor=1/3/5` で描画スモークを確認する。
- [x] B1F/B3F/B5Fのプレビューを保存する。
- [x] `docs/3. 仕様詳細.md` に単色3Dビュー配色を反映する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... phase38 palette preview and draw_3d_view smoke ..."`

---

## Phase 39: Revert Solid Dungeon Palette（完了）

指示書: `docs/archive/plan_20260515_revert_solid_dungeon_palette.md`

優先Issue: Phase 38の茶/グレー系単色配色が不自然だったため撤回する。

### Phase 39.1: 配色撤回（完了）
- [x] `WALL_COLS` をPhase 37相当の `[11, 3, 3, 1]` へ戻す。
- [x] 単色ポリゴンの輪郭線を黒 `0` から `COL_DARK_GRAY` へ戻す。

### Phase 39.2: 検証・同期（完了）
- [x] `assets_loaded=True/False`、`dungeon_floor=1/3/5` で描画スモークを確認する。
- [x] B1F/B3F/B5Fのプレビューを保存する。
- [x] `docs/3. 仕様詳細.md` にPhase 38撤回とPhase 39基準を反映する。

検証コマンド:
- `env SDL_AUDIODRIVER=dummy XDG_RUNTIME_DIR=/tmp python -B -c "... phase39 revert preview and draw_3d_view smoke ..."`

---

## Phase 40: Public Release Preparation（完了）

指示書: `docs/archive/plan_20260921_public_release_prep.md`

### Phase 40.1: リポジトリ名変更
- [x] GitHub上で `games` を `delvoker` へ改名する。
- [x] 改名後にローカルの `origin` を `https://github.com/gantaro-max/delvoker.git` へ更新する。

### Phase 40.2 - 40.6: 公開入口と品質基盤
- [x] `.gitignore` を追加し、Pythonキャッシュ・Pyxel生成物・セーブデータを除外する。
- [x] 追跡済みのPythonキャッシュとPyxel生成スクリーンショットを追跡解除する。
- [x] 機能要件のパーティ人数を `Party.MAX_SIZE = 4` に同期する。
- [x] タイトル、街、ダンジョン、戦闘のREADME用スクリーンショットを無人生成できるようにする。
- [x] Pyxel非依存のデータ、マップ生成、保存処理をpytestで検証する。
- [x] READMEに概要、起動、操作、開発体制、テスト、構成、既知課題を記載する。

### Phase 40.7: 次フェーズ候補の分割方針
- [ ] 状態ごとの `update_*` / `draw_*` を `states/` 配下のモジュールへ移す。
- [ ] 戦闘進行を `logic/battle.py` として切り出す。
- [ ] ショップ、ギルド、マイホームのメニューを `ui/` 配下へ移す。

着手は公開後、CIが安定してから行う。テストなしでの大規模分割は避ける。

### Phase 40.8 - 40.9: CIと記録
- [x] GitHub ActionsにtestとPyxel起動スモークジョブを追加する。
- [x] レビュー追従として、Xvfbの出力を切り離し、起動待機後に撮影する2 step構成へ修正する。
- [x] スクリーンショット生成スクリプトの未使用コールバック保持処理を削除する。
- [x] READMEにCIステータスバッジを追加する。
- [x] `main` へのpush後、Python 3.11 / 3.12のtestとsmokeがすべて成功し、スクリーンショット成果物が保存されることを確認する。
- [x] 指示書をアーカイブし、公開準備の記録を同期する。

## Future Issues (Backlog)

### Issue #01: ダンジョン・バイオームの多様化
- 階層ごとの特殊環境ギミック（視界制限、属性強化ゾーン）。
- マップ生成アルゴリズムの多様化（大部屋、洞窟型）。
- ダンジョン内ランダムイベントの追加（キャンプ、ミニクエスト）。

### Issue #02: ローカライズとユーザビリティ向上
- 日本語フォント導入によるメッセージの日本語化。
- 倉庫の一括移動やショップのまとめ買い等のUI改善。
- インベントリ・ショップ画面のページングやソート機能。

### Issue #03: BGMの差し替えと最終調整
- `assets.pyxres` に適切なBGMデータを設定し、`pyxel.musics[x].set(...)` のコメントアウトを解除。
- 街・ダンジョン・バトルの各BGMを最終調整する。

### Issue #04: 全体テストプレイとバグフィックス（Phase 30へ昇格）
- Phase 30でタスク分解済み。
