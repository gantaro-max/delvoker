# 開発タスク一覧（ロードマップ）

個人開発で挫折を防ぐため、段階的に実装を進めるタスク分解です。

## Phase 1: 基礎プロトタイプ（歩ける・戦える）
- [x] Pyxelの基本プロジェクト作成（画面サイズ、カラーパレットの初期設定）。
- [x] 一人称視点3Dダンジョンの描画テスト（ワイヤーフレーム等で、1マス進む・曲がる処理の実装）。
- [x] 基本的なターン制バトルの構築（味方1人 vs 敵1体で、攻撃してHPを減らし合う基本ロジック）。

## Phase 2: コアループの構築（潜る・戻る・成長する）
- [x] データ構造の設計（アイテムデータ、敵データ、ジョブ・ステータスクラスの作成）。
- [x] 街のメニュー画面の作成と、街⇔ダンジョンの画面遷移の実装。
- [x] **【構造強化】マスターデータのJSON外部化**：アイテム、敵、ジョブの全データをJSONから読み込む仕組みへ移行。
- [x] **【構造強化】UI Windowフレームワークの構築**：装飾枠・アニメーション・タブ切り替えを持つ汎用Windowクラスの実装。
- [x] レベルアップによるステータス自動上昇ロジックの実装（ジョブごとの成長率設定）。
- [x] アイテムインベントリ機能と、装備の着脱処理の実装。

## Phase 3: Delvoker独自の面白さの実装（ハクスラ・NPC・ペナルティ）
- [x] 武器のランダムエンチャント（接頭辞・接尾辞の付与とステータス変化）の実装。
  - [x] `data/enchants.json` の作成（接頭辞・接尾辞データ、ウェイト値）
  - [x] `data.py` に `EnchantedWeapon` クラスを追加（prefix/suffix を保持し `label()` で名前を組み立てる）
  - [x] ドロップ時にウェイト抽選でエンチャントを付与するロジックの実装
  - [x] インベントリ画面でレアリティに応じた表示色（Common:白 / Magic:黄 / Rare:橙 / Cursed:紫）に対応
- [x] エンカウント率の調整と戦闘属性（Attribute）システムの実装。
  - [x] `ENCOUNTER_RATE` を `0.3` → `0.15` に引き下げ（探索ストレス軽減）
  - [x] `WeaponItem` に `attribute` フィールド追加（初期値 `None`）
  - [x] `EnemyDef` に `weaknesses` / `resistances` リスト追加
  - [x] `EnchantedWeapon` で接尾辞の `attribute` を引き継ぐよう対応
  - [x] `Status`（プレイヤー）に `weaknesses` / `resistances` プロパティ追加
  - [x] `_calc_dmg` を更新（弱点×1.5、耐性×0.5、最低ダメージ1保証）
  - [x] `holy` 属性を含む属性システム基盤を構築
- [x] パーティ複数人での戦闘対応と、臨時NPCのAI行動（勝手な行動）ロジックの実装。
- [x] 固有NPCへの昇格条件処理と、昇格による操作権限解放の実装。
  - [x] ギルド画面（STATE_GUILD）でNPCを1000Gで昇格（`is_unique = True`）
  - [x] 昇格済みNPCは戦闘中に `STATE_BATTLE_NPC_CMD` でコマンド選択（Fight/Retreat）
- [x] 全滅時の「ランダムロストルーレット演出」と、アイテム消去処理の実装。
  - [x] `_calc_item_loss()`: インベントリ・装備武器・装備防具からランダム1つ消去
  - [x] 赤フラッシュ演出（`flash_timer = 20`）と `ITEM LOST: ...` ログ表示
- [x] 魔導書（アイテム）使用によるスキル習得・上書きシステムの実装。
  - [x] `GrimoireItem` クラス（`data.py`）と魔導書2種（`items.json`）
  - [x] `_do_use()` でスキル習得、最大4枠、満杯時は最古を自動上書き

## Phase 4: システムの深化（戦闘・探索・成長）
- [x] **属性相性システム（三すくみ）の実装**: fire > ice > poison > fire の相性倍率をダメージ計算に統合。
  - [x] `ATTR_AFFINITY` 定数の追加（`data.py`）
  - [x] `_calc_dmg` をアフィニティ対応に更新（三すくみ×1.5、holy×1.2）
  - [x] `enemies.json` に属性データ追加（Skeleton: ice 属性、Goblin: fire 属性、Bat: holy 弱点）
- [x] **防具エンチャントシステムの実装**
  - [x] `EnchantedArmor(ArmorItem)` クラスの追加（prefix/suffix、attribute耐性）
  - [x] `make_enchanted_armor()` 関数の追加（ウェイト抽選）
  - [x] `enchants.json` に armor_prefixes / armor_suffixes を追加（Sturdy/Light/Heat/Cold/Venom）
  - [x] `Status.resistances` をダイナミックプロパティ化（装備防具の属性を自動追加）
  - [x] インベントリでの `EnchantedArmor` レアリティ表示色対応（magic: 黄）
  - [x] ドロップ処理で防具は `make_enchanted_armor()` を通じてエンチャント抽選
- [x] **戦闘システムの高度化**: 状態異常(Poison/Stun)、ダメージ数値のカラーポップアップ表示。
  - [x] `Status.status_effects = {"poison": 0, "stun": 0}` 追加
  - [x] ポップアップシステム (`add_popup`, `popups` リスト, 20フレームフロート表示)
  - [x] 毒: 敵ターン終了時に2ダメージ、スタン: 行動スキップ + ログ
  - [x] 戦闘画面に `[P]`(緑) / `[S]`(黄) ステータスアイコン表示
  - [x] 敵が状態異常を付与 (Slime: poison 30%, Skeleton: stun 20%)
- [x] **マップ生成の自動化**: ランダム生成(Procedural Generation)ロジック、階段、宝箱ギミックの配置。
  - [x] `Map` クラス追加 (`data.py`): `generate_random`, `visit`, `tile_at`, `set_tile`
  - [x] タイル定数追加 (`TILE_FLOOR/WALL/STAIRS/CHEST`)
  - [x] 部屋と通路アルゴリズム (20×20マップ、最大8部屋)
  - [x] 階段・宝箱の自動配置 (各1つ以上)
  - [x] オートミニマップ (右下、1px/タイル、訪問済みのみ表示)
  - [x] 宝箱インタラクション (エンチャント付きアイテムドロップ、タイル消去)
  - [x] 階段インタラクション (次フロア生成、フロア番号表示)
- [x] **成長システムの拡張**: レベルアップ時のステータスポイント手動割り振り。
  - [x] `Status.bonus_points` 追加 (レベルアップ毎 +3)
  - [x] `Status.mag` 追加 (初期値 2)
  - [x] `STATE_STAT_ALLOC` 状態と Stats メニュー追加
  - [x] STR/DEF/AGI/MAG 割り振りUI (Z: 消費, X: 完了)
- [x] **敵AIの多様化**: AIタイプ分岐 (normal/ranged/support)、ボス予兆システム。
  - [x] `EnemyDef.ai_type` フィールド追加 (ranged/support/normal)
  - [x] `_get_enemy_target()` でAIタイプ別ターゲット選択
  - [x] `EnemyDef.telegraph_message` フィールド追加
  - [x] ボス予兆: 30%確率で警告→次ターン2倍ダメージ (`[POWER]`)
  - [x] `orc_chief` ボス敵追加 (HP:60, DEF:4, support AI, telegraph付き)
  - [x] `skeleton` NPC タイプ追加 (`NPC_TYPES`)
- [x] **部位破壊システム**: 特定部位へのダメージ蓄積によるレアドロップフラグ付与。
  - [x] `EnemyDef.parts` リスト追加（`name`, `hp_ratio`, `weakness`, `drop_flag`）
  - [x] `Enemy.part_hps` / `Enemy.broken_parts` 追加、生成時にHP初期化
  - [x] `STATE_BATTLE_TARGET_PART` 状態追加、`_build_part_targets()` で未破壊部位リスト生成
  - [x] 部位選択UI（`_upd_battle_target_part` / `_draw_battle` 内の TARGET_PART 描画）
  - [x] `_player_attack(part_name)` で部位HPへの適用と `[PART BROKEN: {name}]` ログ
  - [x] `_handle_victory` で破壊済み部位の drop_flag 別枠ドロップ判定（20%）
  - [x] `enemies.json` の `orc_chief` に `Right Arm` 部位データ追加
  - [x] `items.json` に `chief_gauntlet` 追加

## Phase 5: システム拡張と完成度向上
- [x] **NPC個別装備と高度AIの実装**:
  - [x] インベントリからNPCへの装備授与機能（`STATE_INV_GIVE_NPC`）
  - [x] 授与後に旧装備をプレイヤーインベントリへ返却
  - [x] 性格別スキル自律使用AI（reckless:40%, cowardly:80%, normal:20%）
- [x] **町での復活サービスの実装**:
  - [x] `Party.fallen` プロパティ追加（HP ≤ 0 メンバー一覧）
  - [x] `STATE_REVIVE = 13` の追加と `TOWN_MENU` への「Revive」追加
  - [x] `_upd_revive` / `_draw_revive` 実装（費用: Lv×100G、全回復）
- [x] **トラップと環境ギミックの実装**:
  - [x] `TILE_TRAP_SPIKE` タイル追加（踏むと 5〜10 ランダムダメージ）
  - [x] `TILE_TRAP_POISON` タイル追加（踏むと毒 3 ターン付与）
  - [x] マップ生成時にトラップタイルをランダム配置（各部屋 0〜2 個、スタート地点除外）
- [x] **遺品回収イベントの実装**:
  - [x] `Grave` データクラスの定義と `App` での保持 (`self.grave`, `self.is_grave_battle`)
  - [x] 全滅時に座標・フロア・アイテムを `Grave` に保存するロジック (`_enemy_turn` / `_calc_item_loss` 修正)
  - [x] `Map.generate_random` での `TILE_GRAVE` 配置 (grave/current_floor 引数追加)
  - [x] 遺品回収バトルの実装 (Revenant 追加、`_upd_dungeon` タイル判定、`_handle_victory` 回収処理、ミニマップ COL_PINK 表示)
- [x] **拠点（マイホーム）の永続強化（実装済み）**: `STATE_HOME = 15`、倉庫 Warehouse（手持ち↔STORAGE 移動）、Renovate（+5枠拡張×5段階）、Training（STR/DEF/MAG 永続強化）を実装。`Player.perm_stats` を攻撃・防御計算に反映。
- [x] クリア目標（ダンジョンコア/マスターとの最終決戦、任意テレポートスキルの付与）の実装。
  - [x] `MAX_FLOOR = 5` 定数追加（B5Fが最深部）
  - [x] B5F階段踏み込み時に `dungeon_master` ボス戦を開始するロジック
  - [x] `enemies.json` に `dungeon_master` 追加（HP:150、全属性耐性、予兆付き）
  - [x] `STATE_ENDING = 16` / `_upd_ending` / `_draw_ending` の実装（最終スタッツ表示）
  - [x] ボス撃破後に `_handle_victory` から `STATE_ENDING` へ遷移
  - [x] ボス戦では逃走を100%失敗にするガード処理
  - [x] `Skill.is_utility` フラグ追加 / `GrimoireItem.is_utility` 追加
  - [x] `items.json` に `grimoire_return`（Teleport スキル習得、800G）追加
  - [x] `STATE_DUNGEON_SKILL = 17` / `_upd_dungeon_skill` / `_draw_dungeon_skill` の実装（Sキー）
  - [x] Teleport 使用時（MP:10消費）でダンジョンから即 `STATE_TOWN` 遷移
## Phase 6: コンテンツ拡張（実装済み）
- [x] **階層拡張とボス再編**:
  - [x] `MAX_FLOOR = 10` に更新（B10Fが最深部）
  - [x] B5F階段踏み込み時に `dungeon_master`（中ボス）戦、撃破後に B6F へ進行
  - [x] B10F階段踏み込み時に `archdemon`（最終ボス）戦、撃破後にエンディング
  - [x] `_try_flee` で `dungeon_master` および `archdemon` への逃走を禁止
- [x] **新規モンスター追加** (`enemies.json`):
  - [x] `wraith` (HP:35, holy弱点, fire/ice耐性, ranged AI)
  - [x] `golem` (HP:80, DEF:5, normal AI)
  - [x] `mimic` (HP:30, DEF:3, normal AI)
  - [x] `wyvern` (HP:50, AGI高, ice弱点, ranged AI)
  - [x] `archdemon` (HP:300, DEF:8, 全属性耐性, 予兆付き最終ボス)
- [x] **新規アイテム追加** (`items.json`):
  - [x] `steel_sword` (2d8+3), `mithril_sword` (2d10+5)
  - [x] `steel_plate` (DEF+6), `antidote` (毒解除)
  - [x] `grimoire_ice` (Ice Lance, MP:5, 威力15), `grimoire_poison` (Poison Mist, MP:4, 威力10)
- [x] **階層別ドロップティア実装** (`main.py`):
  - [x] `_drop_pools(floor)` 関数: B1-3/B4-6/B7-10 の三段階
  - [x] `_open_chest` と `_handle_victory` でフロア別ティアを参照
- [x] **階層別エンカウント実装** (`main.py`):
  - [x] `_encounter_pool(floor)` 関数: B1-3/B4-6/B7-10 の三段階
  - [x] `_start_battle` と `_spawn_dungeon_npcs` でフロア別プールを使用
- [x] **ミミック実装** (`main.py`):
  - [x] TILE_CHEST 踏み込み時に20%確率でミミック戦が発生
- [x] **状態異常治療** (`main.py`, `data.py`):
  - [x] `ConsumableItem.cure_status` フィールド追加
  - [x] `_do_use` で `cure_status` を参照し `status_effects` をリセット
- [x] **ショップ拡充**: `antidote`, `grimoire_ice`, `grimoire_poison` を `SHOP_KEYS` に追加
- [ ] SE（効果音）とBGM（レトロチップチューン）の作成・組み込み。
- [ ] 全体的なバグフィックスと最終テストプレイ。