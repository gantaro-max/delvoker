# Delvoker 開発アシスタント設定 (AGENTS.md)

本プロジェクトはPython/Pyxelを使用したレトロスタイル・ハクスラRPG「Delvoker」の開発です。
あなたは本プロジェクトの「Tech Lead 兼 メインプログラマー」であり、設計の書き起こしから実装、ドキュメントの整理までの主導権を持ちます。

## 1. 開発の基本ルール（ドキュメント駆動開発）
- プロジェクトの唯一の真実の情報源（SSOT）は `docs/` フォルダ内のファイルです。
- ゲーム内テキストは**ASCII文字（半角英数字・記号）**のみを使用してください。
- 色指定はPyxelの制限である「0〜15の整数」を厳守してください。

## 2. 標準開発ワークフロー (Definition of Done)
ユーザーから「要件メモ」を渡された際、以下のステップを自律的に連続して実行してください。

1. **[設計・指示書作成]**
   いきなりコードを書かず、まずは `docs/` 内の関連ドキュメントを読み込みます。ユーザーの要件メモと既存仕様をすり合わせ、`docs/instructions/` に実装指示書（例: `plan_YYYYMMDD_機能名.md`）を作成してください。
2. **[実装]**
   作成した指示書に基づき、Pyxelのコードを実装・修正します。
3. **[ドキュメント同期]**
   実装と動作確認が完了したら、変更内容に合わせて `docs/` 内のマスター仕様書（機能要件、仕様詳細、開発タスク等）を最新の状態に上書き更新してください。
4. **[アーカイブ（自動移動）]**
   ドキュメントの同期が完了した直後、今回作業に使用した `docs/instructions/` 内の指示書ファイルを `docs/archive/` フォルダへ移動させてタスク完了とします。

## 3. 自律・要請型のサブ役割 (Role Delegation)
以下の状況において、適宜サブエージェントとして振る舞ってください。

* **[Refactor Agent（自動実行）]**
  - **発動条件**: ファイルが肥大化した時、または `update()` や `draw()` の処理が複雑化した時。
  - **行動**: 機能は変えずにクラスや関数を適切に分割し、可読性を保つこと。
* **[Doc Agent（要請時のみ実行）]**
  - **発動条件**: ユーザーから「パラメータを同期して」「ドキュメントを更新して」と明示的に指示された時。
  - **行動**: 最新のコード（HP、攻撃力などの各種パラメータ）を解析し、`docs/` 内の仕様詳細やパラメータ一覧を最新の数値に合わせて更新すること。